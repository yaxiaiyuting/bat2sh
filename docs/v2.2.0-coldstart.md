# bat2sh v2.2.0 冷启动自检（只读）

> 会话：v2.2.0 —— goto 高级形态 + P5 并入 + sc + 注册表写。
> **实测优先协议**：roadmap 估算默认不可靠，逐项实测后决定（纪律 18）。
> 分支 `v2.2.0-goto-adv`；锁定起点 HEAD `318d543`。本文件为**只读自检产出**，不进入实现。

---

## 1. 前置确认（硬性五条）

| # | 要求 | 实测 | 判定 |
| :--- | :--- | :--- | :---: |
| 1 | v2.1.0 已发布（`git tag -l v2.1.0` 有输出） | `v2.1.0` → `ab31e9d`（bump commit） | ✅ |
| 2 | 起点 HEAD = `318d543` | `main` = `318d543` = `origin/main` | ✅ |
| 3 | `git status` 干净 | `git status --porcelain` 空 | ✅ |
| 4 | `pytest -q` 全绿 | **1436 passed**（80.31s） | ✅ |
| 5 | 仪器复现 151/149/104/19/85/897/崩溃 0 | **逐项一致**（见 §1.1） | ✅ |

### 1.1 仪器复跑（`tools/corpus-analysis/measure.py`）

```bash
python3 tools/corpus-analysis/measure.py --corpus ~/下载/非常批处理 --out /tmp/bat2sh-v220-baseline --json
```

| 指标 | 要求 | 实测 | 判定 |
| :--- | ---: | ---: | :---: |
| corpus | 151 | **151** | ✅ |
| syntax_ok | 149 | **149** | ✅ |
| rc0 | 104 | **104** | ✅ |
| strict | 19 | **19** | ✅ |
| degraded | 85 | **85** | ✅ |
| degraded_todos | 897 | **897** | ✅ |
| crash | 0 | **0** | ✅ |

> **口径澄清（实测发现）**：`degraded=85` 中有 1 个是**仅 marker**文件 —— `改名复制文件.cmd`
> （`rc==0`、`todo_count==0`、`markers==1`）。其 `# TODO` 来自 **error 层**（`label_in_block`：
> 标签 `:copy_file` 位于控制块内），非 todo 层。故 `degraded(rc0+todo>0)=84`、`+1 marker == 85`。

### 1.2 分支

- 分支名：`v2.2.0-goto-adv`；起点 HEAD：**`318d543293f8614e354b437c3e964d598869593c`**
- HEAD 锁：`/tmp/bat2sh-v220/head.lock`

---

## 2. 读了哪些文档（按序）

1. `docs/PROJECT-OVERVIEW.md`（纪律 1–17；§5.3 注册表写；§5.7 C4；§5.9 CFG）
2. `docs/2.x-roadmap-research.md`（§2.A goto、§2.B sc、§4.3 v2.2.0、§7 session 表 P6/P7/B1/R1）
3. `docs/v2.1.0-report.md`（P5 估算错 3–4→0 的教训）
4. `docs/v2.0.0-report.md`（「语法 4→1」估算错）
5. `docs/session-c4-report.md` / `session-c4-design.md`（C4 台账；Phase 3 回跳、Phase 4 块内）
6. `docs/v1.11.0-reclassification.md`（注册表写 29/20/11 逐条归属）
7. `docs/v2.1.0-review.md` / `v2.1.0-coldstart.md`（P5 退回依据、判据）

---

## 3. 用自己的话复述

### 3.1 v2.2.0 的 roadmap 提议范围（§4.3）

| 项 | 内容 | roadmap 预期 |
| :--- | :--- | :--- |
| ① goto 高级 | `backward_loop` → `while/until`（446/28）；`in_block_goto`（457/8） | 翻转 **0–2**（「高级 goto 多为多设施文件」） |
| ② P5 并入 | `redundant_goto` + 顶层 `forward_skip` | v2.1.0 实测 **0** |
| ③ sc→systemctl | `service` 名称表 + 动词映射（171 TODO） | 翻转 **0**；价值 = 结构化诚实 TODO |
| ④ 注册表写 | 逐键映射（29 TODO / 20 文件 / 11 单设施） | 「11 个可能翻转（若映射被接受）——但语义近似风险高」 |
| 依赖 | v2.0.0 / v2.1.0（CFG 数据模型已就位） | — |

### 3.2 实测优先协议（纪律 18，本 session 特有）

> **roadmap 估算默认不可靠**：v1.8.1 起已有**六次估算错**（见 §4.2）。本 session **不采信任何
> 未实测的翻转数字**；对每一项做：**TODO 数 → 逐文件可翻转数（不数 TODO）→ 宿主集中度 →
> 是否需块结构发射（053）→ 是否需 CFG 高级设施**，再裁定「可做 / 诚实 TODO 化 / 归 v2.3+ /
> 终态 / 不做」。**只有通过实测的项才实现。**

---

## 4. 前提核对

### 4.1 P5 是否还有候选？

**无。** v2.1.0 已实测 `redundant_goto` 27/5、顶层 `forward_skip` 551/26 的安全子集**翻转 0**
（独立 no-op 宿主是语法失败文件；其余需行级重写 / 块结构发射）。本 session 用 CFG 复跑确认：
`forward_skip` 的 rc==0 degraded 文件**无一为单桶**（详见 `v2.2.0-reality-check.md` §2.C）。
→ **P5 不做**（维持 v2.1.0 裁定）。

### 4.2 sc 171 TODO 的宿主分解与真实翻转可能

实测（`measure.json` + 只读探针）：rc==0 文件中 sc TODO = **171**（`系统优化.bat` 170 + `备份服务.bat` 1）。
- 动作分布：`config` 165 / `description` 2 / `query` 2 / `create` 1 / `delete` 1；
- distinct 服务名 **94**（91 字面 + 3 占位符 `${a}/${g}/${m}`）；
- **单设施文件（全部 TODO = sc）＝ 0** → **翻转 0**（`系统优化` 另有 goto/sc/registry/l4obj… 443 TODOs）。

### 4.3 注册表写是否仍归 2.x？

**是——且更准确地说：归「终态（保持诚实 TODO）」。** v1.11.0-reclassification §1 已逐条判定
29/29 为写类/导出（Linux 无统一可写注册表）。本 session 实测确认：注册表写**当前已输出结构化
诚实 TODO**（`# TODO[REG] op=write key="…" value="…"`，`batch.py:3148-3168`），11 个单设施文件
**无需改动**；映射到配置文件属语义近似（违反纪律 7）→ **不发明映射**。

### 4.4 是否触发暂停？

**否。** 前置五条全过；无并发写入；无前提冲突。但**roadmap 的 v2.2.0 预期（翻转 0–2 + 11 注册表）
经实测进一步收敛为「翻转 0」**（详见 reality-check）。

---

## 5. 偏离披露（纪律 3/4）

| # | 项 | 说明 |
| :--- | :--- | :--- |
| 1 | roadmap 服务名数记 93 | 实测 rc==0 文件 distinct 服务名 **94**（含 3 个占位符；字面 91）；语料全域 103。量级不变 |
| 2 | roadmap 注册表「11 翻转」 | 实测：11 个 rc==0 degraded 单设施文件存在，但**无安全映射**（Linux 无注册表）且**已结构化** → 0 翻转 |
| 3 | `degraded 85` 口径 | 含 1 个仅 marker（error 层 `label_in_block`）文件；非 todo 层（§1.1） |

---

## 6. 记录 HEAD

| 项 | 值 |
| :--- | :--- |
| 分支 | `v2.2.0-goto-adv` |
| **起点 HEAD** | `318d543293f8614e354b437c3e964d598869593c` |
| tag `v2.1.0` | `ab31e9d`（bump commit） |
| 退路（回滚点） | `318d543`（= `main`） |

---

> **本文件为只读自检产出。** 下一步：`docs/v2.2.0-reality-check.md`（核心产出：逐项实测）。

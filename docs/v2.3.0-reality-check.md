# bat2sh v2.3.0 逐项实测（实测优先 · 核心产出）

> 会话：v2.3.0。分支 `v2.3.0-goto-cfg`；起点 HEAD `b12e336`。
> **本文件是本 session 的核心产出**：对 v2.3.0 提议项做**实测优先**裁定（纪律 18）。
> 全程只读：探针在 `/tmp/bat2sh-v230-*.py`（未入仓）。
> **结论先行：全部提议项实测翻转 0；`goto CFG 高级` = 「语义完整但翻转 0」→ 触发 2.x 收尾评估。**

---

## 0. 实测协议

| 维度 | 说明 | 仪器 |
| :--- | :--- | :--- |
| **TODO 数** | 该设施在 `report.todos` 的条数 | `measure.json` |
| **可翻转文件数** | 「全部 TODO 同属该设施」且 `rc==0` 的 degraded 文件数 | 只读探针（逐文件分桶） |
| **宿主集中度** | 是否集中在少数多设施文件 | 探针 + `measure.json` |
| **是否触 053/A1** | 是否须改 `_label_line`/块发射/A1 热路径 | 代码阅读 |
| **是否需 CFG 高级** | 是否依赖多入口/支配/循环区间识别 | `core/cfg.py` + 探针 |

仪器先验证：`measure.py` 复现 151/149/104/19/85/897/0；`core/cfg.py` 形态 7/7 与 C4 台账一致。

---

## 1. goto 高级形态

### A. 回跳（`backward_loop` → `while/until`）

| 维度 | 实测 |
| :--- | :--- |
| CFG 语句 / 文件 | **446 / 28** |
| rc==0 degraded 含回跳 | **29 文件** |
| **可翻转文件** | **1** —— `显示所有分区的剩余空间（以MB为单位）.cmd`（2 TODO） |
| 该文件 goto | `goto Main`（顶层回跳）+ `goto loop`（函数内自环）；**无多入口标签**（incoming main:1 / loop:1） |
| 宿主集中度 | 高：`系统优化.bat`(443, 多设施)、`史上最牛X…bat`(3203, 语法失败)；余为多设施 |
| 触 053？ | **是** —— 回跳→循环须在**标签处发射 `while`** → 直接改 `_label_line`（053 已修路径清单） |
| 需 CFG 高级？ | 是（循环区间 + 出口条件 `if not "%num1%"=="" goto loop`） |
| **裁定** | **不做**（触 053 无安全方案；翻转仅 1）→ **归终态（仅 T1 重评）** |

### B. 块内（`in_block_goto`）

| 维度 | 实测 |
| :--- | :--- |
| CFG 语句 / 文件 | **457 / 8** |
| **可翻转文件** | **0**（无 rc==0 degraded 单桶文件） |
| 宿主 | `系统优化`(443)、`史上最牛X`(语法失败)、`文件备份器V2.3`(76)、`一键转移桌面…`(14)、`文件归类整理器`(9)、`按软件分类建立文件夹`(4, rc=127)、`xp下确定最后的盘符`(2)、`判断分区格式`(2) |
| 触 053？ | **是**（与 053 同域：`for`/`if` 体含 goto） |
| 需 CFG 高级？ | 是（块内 break/continue 映射 + 块栈配平） |
| **裁定** | **不做**（0 翻转；053 同域）→ **归终态（仅 T1 重评）** |

### C. goto CFG 高级（多入口/多出口块结构）

| 维度 | 实测 |
| :--- | :--- |
| 多入口标签文件 | **15**（`史上最牛X` 95 个、`系统优化` 35、`文件备份器V2.3` 11、`文件夹伪装` 9…） |
| **这些文件中有 rc==0 degraded 单桶者？** | **0** —— 15 个多入口文件**全部**是多设施或语法失败文件 |
| 唯一 goto 单桶文件是否依赖多入口？ | **否**（`显示所有分区` incoming main:1 / loop:1，无多入口） |
| **裁定** | **「语义完整但翻转 0」** —— CFG 高级能力**不改变任何文件的翻转**；且其落地仍须触 053（`_label_line`）→ **归终态（仅 T1 重评）** |

> **关键实测**：goto 的唯一可翻转文件**不需要 CFG 高级**（无多入口/多出口）；它被**纯 053 阻塞**。
> 因此「投入 CFG 高级（15–25 人日）」**不可能换来翻转 > 0**。

---

## 2. B5 括号跨行累积

| 维度 | 实测 |
| :--- | :--- |
| 目标缺陷 | `史上最牛X…bat` 源 6241-6242：`for /f … in ('wmic … get` ⏎ `Manufacturer^,…/value') do (` —— **跨物理行**（无 `^` 续行），当前 `_logical_lines` 不累积 |
| 现状症状 | 续行被当独立逻辑行**原样发射**（含未配平 `)`）→ `bash -n: 行 7706 未预期 )` |
| 定向探针（`_logical_lines` 对 `for /f` 未闭合单引号累积） | **churn = 1 文件**（仅 `史上最牛X`）；**语法仍失败** → 暴露**第二处独立缺陷**（生成 15284，`.reg` 的 `echo` 引号/内容失衡，与 `for/f` 无关） |
| 指标收益 | **0**（无语法通过、无翻转；且 `史上最牛X` 即使修复语法仍为 3203 TODO 的 degraded） |
| 触 053/A1？ | `_logical_lines` 不在 053 清单，但是**解析层核心**（全部转换共享）；改动面不可控 |
| **裁定** | **不做**（实测价值 0）→ **归终态/解析层重写（仅 T1 重评）** |

> **附**：`合并文本`×3 的 `for /f` 是**变量选项**（`"%k%"` = `` 或 `skip=N`）+ 循环体含 `goto`
> （`batch.py:2424` 整行 TODO）—— 与 B5 的「跨物理行」**不是同一机制**，B5 对其无影响。

---

## 3. 注册表写逐键（29 TODO）

| 维度 | 实测 |
| :--- | :--- |
| registry TODO | **29 / 20 文件 / 11 单设施**（v1.11.0-reclassification §1 逐条：写 26 + 导出 3） |
| rc==0 degraded 单桶 registry 文件 | **11**（`lockcmd.bat`、`不显示扩展名/隐藏文件`、`显示扩展名/隐藏文件`、`删除历史记录`、`批处理生成注册表项`、`新建带模板的批处理`、`导入文件夹内所有注册表项`、`导出注册表的键值2`、`锁注册表.BAT`） |
| 当前产物 | **已结构化**：`# TODO[REG] op=write|delete|import|export key="…" value="…"`（`batch.py:3148-3168`） |
| 逐键映射可行性 | **全部不可**：Linux 无统一可写注册表；`Explorer\Advanced` / `ShellNew` / `Policies\System` / `ContextMenuHandlers` 等均为 Windows 专有；映射到配置文件属**语义近似** → 违反纪律 7 |
| **裁定** | **终态（维持）**：不发明映射；已结构化，无需改动 |

---

## 4. 汇总（roadmap 估算 vs 实测）

| 项 | roadmap 估算 | **实测翻转** | 可翻转文件 | 触 053/A1 | 需 CFG 高级 | **裁定** |
| :--- | :--- | :---: | :--- | :---: | :---: | :--- |
| goto 回跳 | 0–2 | **0**（候选 1） | `显示所有分区的剩余空间` | **是** | 是（但该文件不需要） | **不做 → 终态（T1 重评）** |
| goto 块内 | 0–2 | **0** | — | **是** | 是 | **不做 → 终态（T1 重评）** |
| goto CFG 高级（多入口） | ？ | **0** | —（15 多入口文件均非单桶） | **是** | 是 | **语义完整但翻转 0 → 终态（T1 重评）** |
| B5 括号跨行 | ？ | **0** | — | 解析层核心 | 否 | **不做 → 终态/解析层重写** |
| 注册表写逐键 | 「11 可能翻转」 | **0** | 11 单设施但无安全映射 | 否 | 否 | **终态（维持）** |

**总判定：全部提议项翻转 0；`goto CFG 高级` = 「语义完整但翻转 0」→ 触发 2.x 收尾评估。**

---

## 5. 偏离披露（纪律 3/4）

| # | 项 | 说明 |
| :--- | :--- | :--- |
| 1 | B5 探针 | 定向 `_logical_lines` 累积探针（`/tmp/bat2sh-v230-b5probe.py`）为**可行性实验**，未入仓；结论「单独不足」以第二缺陷实测为准 |
| 2 | roadmap「注册表 11 翻转」 | 实测无安全映射且已结构化 → 0 翻转 |
| 3 | CFG 多入口文件数 | 实测 **15**（v2.1.0 记 15）；其中 0 个 rc==0 单桶 |
| 4 | `degraded=85` 口径 | 含 1 个仅 marker（error 层 `label_in_block`）文件 |

---

> **本文件为只读实测产出。** 裁定见 `docs/v2.3.0-review.md`；2.x 收尾评估见 `docs/v2.3.0-2x-closure.md`。

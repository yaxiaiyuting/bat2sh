# bat2sh v2.3.0 报告（goto CFG 高级实测 + 2.x 收尾）

> 交付：`docs/v2.3.0-coldstart.md`、`docs/v2.3.0-reality-check.md`（**核心产出**）、
> `docs/v2.3.0-review.md`、**`docs/v2.3.0-2x-closure.md`（2.x 收尾声明）**、
> `docs/releases/v2.3.0.md`、本文件。**零代码改动。**

| 项 | 内容 |
| :--- | :--- |
| **分支名** | `v2.3.0-goto-cfg` |
| **起点 HEAD** | `b12e336`（= v2.2.0 终点；= `main` = `origin/main`） |
| **代码/文档终点 HEAD** | （回填） |
| **tag** | `v2.3.0` |
| **发布终点 HEAD（= tag 目标）** | （回填） |
| **tag 后 pkg sync HEAD** | （回填） |

---

## 1. 逐项实测结果（roadmap 估算 vs 实测）

| 项 | roadmap 估算 | **实测翻转** | 关键事实 | 裁定 |
| :--- | :--- | :---: | :--- | :--- |
| goto 回跳 | 0–2 | **0**（候选 1） | `446/28`；候选须触 `_label_line`(053) | 终态（T1 重评） |
| goto 块内 | 0–2 | **0** | `457/8`；0 rc==0 单桶；053 同域 | 终态（T1 重评） |
| **goto CFG 高级**（多入口/多出口） | ？ | **0** | 15 多入口文件全为多设施/语法失败；唯一 goto 单桶文件**无多入口** | **语义完整但翻转 0 → 终态（T1 重评）** |
| B5 括号跨行 | ？ | **0** | 定向探针 churn=1（`史上最牛X`）、**语法仍失败**（第二缺陷独立） | 终态（解析层重写） |
| 注册表写逐键 | 「11 可能翻转」 | **0** | 29 全写类/导出；无安全映射；已结构化 | 终态（维持） |

**实测翻转总数 = 0 → 触发 2.x 收尾评估（任务书情形 B）。**

---

## 2. 完成了什么（逐条）

1. **冷启动自检**（`v2.3.0-coldstart.md`）：前置五条全过；仪器复现；复述 2.x 结构性事实（三重点趋 0）。
2. **逐项实测（核心）**（`v2.3.0-reality-check.md`）：goto 回跳/块内/CFG 高级/B5/注册表写逐项给出
   TODO 数 + 可翻转文件数 + 宿主集中度 + 053/CFG 依赖 + 裁定。
3. **自我审阅与裁定**（`v2.3.0-review.md`）：情形 B 判定；范围/风险/止损/退路/验收；裁定 = 只交实测 + 收尾。
4. **2.x 收尾评估**（`v2.3.0-2x-closure.md`）：三条件全 ✓ + 6 项重点逐条证据链 +
   goto CFG 高级归属（终态/T1 重评）+ 3.x 范围草案 + PS 解冻评估。
5. **发布**：（回填）preflight → FF 合并 `main` → bump → tag → push → tag 上 CI →
   `PKGBUILD`/`.SRCINFO` 同步 + `makepkg` → GitHub Release → 本机安装验证。

---

## 3. 实测翻转数（引用仪器）

**0 文件翻转**（本版零代码改动）。

- 仪器：`tools/corpus-analysis/measure.py`；基线（`b12e336`）vs 本版 summary 逐字段一致
  （151/149/104/19/85/897/0）。
- 转换产物**逐字节 0 diff**（零代码改动）。

---

## 4. 2.x 收尾判定

| 条件 | 判定 | 依据 |
| :--- | :---: | :--- |
| (a) 所有剩余失败有明确归属 | **✓** | 语法失败 2/2 逐条；degraded 85 全按桶归属；无「无归属」项 |
| (b) 无「可修但未修」（实测维度） | **✓** | 2.x 可做且翻转>0 集合 = 5（已全做）；三轮实测可做翻转全 0 |
| (c) 指标稳定且无提升空间 | **✓** | `degraded = 85` = 实测下界；strict 19 / rc0 104 / 崩溃 0 |

**→ 2.x 收尾成立，冻结维护模式。**

---

## 5. goto CFG 高级归属

**终态（仅 T1 真实需求触发重评）。**
理由：实测翻转 0（多入口文件全非单桶；唯一可翻转文件无多入口、纯 053 阻塞）；成本 15–25 人日换 0 翻转。
**不静默归 3.x，也不无条件归终态** —— 明确标注 T1 重评条件与起步设施（`core/cfg.py` + `tools/cfg`）。

---

## 6. 是否触及 053 / A1

**否。** 本版**零代码改动**（纯 docs）；`core/batch.py` / `rules.py` / `core/cfg.py` 等相对 `b12e336` 零 diff。
证据：151 语料产物逐字节 0 diff。

---

## 7. 下一版起点

| 项 | 值 |
| :--- | :--- |
| **下一版起点 HEAD** | 本回填 commit 的 HEAD（v2.3.0 tag 后 docs 回填；以 `git rev-parse HEAD` 为准）；tag `v2.3.0` 见回填 |
| 模式 | **2.x 维护模式**（同 1.x） |
| **3.x 唯一大方向** | **PS 解冻**（需求驱动，未触发 T3） |
| 条件性方向 | 解析层重写（仅 T1） |
| 终态（不进入 3.x） | goto（全部）/ B5 / 注册表写 / 名称映射 / B2 输出适配 / L4 / thirdparty |

---

## 8. 已知限制

- goto 高级、B5、注册表写、PS 均**未实现**，且均有**实测依据 + 明确归属**（`v2.3.0-2x-closure.md` §3）。
- `史上最牛X…bat` 仍语法失败（多缺陷解析层）；`运行剪贴板中的bat.bat` 终态。
- PS 维持实验性支持（60 PS 语料语法 56/60；运行时语义 73.7% 实验口径）。
- 2.x 启动条件（T1/T2/T3 需求驱动）不变。

---

> **本报告为 v2.3.0 最终产出。**（HEAD/tag/安装验证待回填。）

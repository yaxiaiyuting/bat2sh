# bat2sh v2.1.0 报告（CFG 只读数据模型）

> 交付：`docs/v2.1.0-coldstart.md`、`docs/v2.1.0-review.md`、`docs/v2.1.0-breaking-changes.md`、
> `docs/releases/v2.1.0.md`、本文件；代码 `core/cfg.py` + `tools/cfg/*` + `tests/test_cfg.py`。

| 项 | 内容 |
| :--- | :--- |
| **分支名** | `v2.1.0-cfg` |
| **起点 HEAD** | `d901b5c`（任务书记 `a227908`；实测 `main` = `d901b5c` = `a227908` + 1 docs-only commit；与 `origin/main` 一致） |
| **代码/文档终点 HEAD** | （回填） |
| **tag** | `v2.1.0` |
| **发布终点 HEAD（= tag 目标）** | （回填） |
| **tag 后 pkg sync HEAD** | （回填） |

---

## 1. 完成了什么（逐条）

1. **冷启动自检**（`v2.1.0-coldstart.md`）：前置五条核对（tag/pytest 1412/仪器复现/干净树全过）；
   读 8 份文档；复述 P4/P5 与依赖；前提核对**实测证伪路线图「翻转 ≈3–4」**（实为 0）；5 条偏离披露。
2. **自我审阅与裁定**（`v2.1.0-review.md`）：范围/风险/最小复现/止损/退路/替代/原则性验收；
   裁定 = **只交 P4，P5/B5 显式归 v2.2.0**；实现后回填最终指标与逐字节 diff。
3. **破坏性变更声明**（`v2.1.0-breaking-changes.md`）：总则 + 逐项 + 实测回填（**无破坏性变更**）。
4. **只读诊断（实现前，纪律 2/9）**：C4 台账复跑 8/8 OK（goto 1583；TODO 1364/39）；
   自建只读探针（`/tmp/bat2sh-v210-probe.py`，未入仓）定位 redundant / 顶层 forward_skip 宿主，
   交叉 `measure.json` 证明 **P5 翻转 = 0**。
5. **P4 实现**（commit `6f52d67`）：`core/cfg.py`（标签表 + goto 边 + `validate_cfg`）+ `tools/cfg/`（只读报告）。
6. **P4 测试**（commit `d96feea`）：`tests/test_cfg.py` 24 条（不依赖外部语料）。
7. **文档**（commit `6a03385` + 本批）：coldstart / review / breaking-changes / release notes / 本报告。
8. **发布**：（回填）merge main → bump `2.1.0` → `release-preflight.sh` → tag → push → tag 上 CI →
   `PKGBUILD`/`.SRCINFO` 同步 + `makepkg` 验证 → GitHub Release → 本机安装验证。

---

## 2. 实测翻转数（引用仪器）

**0 文件翻转**（本版零转换改动）。

- 仪器：`tools/corpus-analysis/measure.py`；基线 vs 本版 summary 逐字段一致；
  149 个 `script.sh` **聚合 sha256 一致**。
- 路线图估算「翻转 ≈3–4」**被实测证伪**（coldstart §4.2 / review §1.2）。
- P5 未实现 → 无翻转（如实记录，不硬凑）。

---

## 3. 未完成什么（逐条 + 原因）

| 未完成项 | 原因 | 归属 |
| :--- | :--- | :--- |
| **P5 `redundant_goto` 消除** | 实测 0 翻转；独立 no-op 宿主为语法失败文件（产物零影响）；其余需行级重写（触 `_convert_if`/errorlevel） | v2.2.0 |
| **P5 顶层 `forward_skip`** | 实测 0 翻转；转换需块结构发射（053 同域）；置信度 D（多入口） | v2.2.0 |
| **B5 括号跨行累积** | 可选且触发条件不满足；触解析层咽喉 | v2.2+ |
| **P3b LF-1**（来自 v2.0.0） | 触 `_convert_for`（053 已修路径） | v2.2+ |
| `史上最牛X…bat` 语法失败 | 多物理行 `for /f` 泄漏 + `echo` 引号（B5/解析层重写） | v2.2+ |

---

## 4. 指标对比

| 指标 | v2.0.0 | **v2.1.0** | 判定 |
| :--- | ---: | ---: | :--- |
| 语料 / 语法通过 | 151 / 149 | 151 / **149** | ✅ 持平 |
| rc==0（原始） | 104 | **104** | ✅ 持平 |
| 功能完好（strict） | 19 | **19** | ✅ 不降 |
| degraded | 85 | **85** | ✅ 持平 |
| degraded TODO | 897 | 897 | ✅ 持平 |
| 崩溃 / 超时 | 0 / 0 | **0 / 0** | ✅ |
| pytest | 1412 | **1436** | ✅ +24 |
| examples tracked 对漂移 | 0 | **0** | ✅ |
| `validate_cfg`（151 语料） | 不存在 | **0 问题** | ✅ 新增 |
| `tools/cfg` 台账核对 | 不存在 | **7/7 OK** | ✅ 新增 |
| **转换产物逐字节 diff** | — | **0 文件** | ✅ 零转换改动 |

**逐文件 diff**：仅 2 个**沙箱副作用产物**与 stderr 中的**绝对路径**不同；
149 个 `script.sh` 全部逐字节一致（聚合 sha256 `6f56218d…`）。

---

## 5. wine harness

| 版本 | 结果 |
| :--- | :--- |
| v2.0.0 | 6/6 MATCH |
| **v2.1.0** | **6/6 MATCH**（`pytest tests/test_oracle_wine.py -q` = 8 passed） |

---

## 6. 破坏性变更

**无**（纯新增只读设施；零产物变化；CLI/API 未破坏；无静默错）。
详见 `docs/v2.1.0-breaking-changes.md` §4。

---

## 7. 是否触及 053 / A1 路径

**否。** 本版**零转换改动**：`core/batch.py` / `rules.py` 相对 `d901b5c` **零 diff**；
新增 `core/cfg.py` 为纯只读模块（不 import `core.batch`、不被转换器调用，`ast` 测试守护）。
证据：151 语料产物逐字节 0 diff；wine 6/6。

---

## 8. 下一版起点（v2.2.0）

| 项 | 值 |
| :--- | :--- |
| **v2.2.0 起点 HEAD** | （回填：v2.1.0 tag 后 docs 回填 commit；以 `git rev-parse HEAD` 为准） |
| 建议分支名 | `v2.2.0-goto-adv`（从 v2.1.0 终点累积开分支） |
| 前置设施 | `core/cfg.py` + `tools/cfg`（CFG 数据模型）；`core/control_flow.py`（台账）；wine harness 6/6 |
| v2.2.0 范围建议 | goto 高级（回跳→循环 / 块内 goto）+ P5 简单形态并入 + `sc→systemctl` 服务表 + 注册表写映射 |

---

## 9. 已知限制

见 `docs/releases/v2.1.0.md` §5（CFG 括号净值近似；重复标签语义未断言；goto 简单形态仍为诚实 TODO；
LF-1 未修；B5 未做）。

---

> **本报告为 v2.1.0 最终产出。**（HEAD/tag/安装验证待回填。）

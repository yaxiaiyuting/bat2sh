# bat2sh v1.11.0 —— 仪器验证与前提再核（步骤一，只读）

> 会话：C2 收窄执行 + 归属重判 + 标准 3 修订。
> 分支 `v1.11.0-c2`；起点 HEAD **`3ed0d9d`**。
> 本文件为**只读诊断产出**：固化测量仪器并复核基线；不改产品代码。
> **纪律 9（强化）**：测量工具必须可复现，**不可复现的数字视为无效**。

---

## 0. 前置确认（任务书第一节，5 项）

| # | 条件 | 实测 | 判定 |
| :--- | :--- | :--- | :---: |
| 1 | `git tag -l "v1.10.0"` 有输出 | `v1.10.0`（annotated） | ✅ |
| 2 | 起点 HEAD = `3ed0d9d` | `3ed0d9d` | ✅ |
| 3 | `git status` 干净 | **有 2 个未跟踪文档**（上一 session 的交付物 `docs/v1.11.0-coldstart.md`、`docs/v1.11.0-review.md`） | ⚠️ **见 §0.1** |
| 4 | `pytest -q` 全绿 | **1371 passed**（79.68s） | ✅ |
| 5 | 重建仪器并复现基线 | 见 §1 / §2 | ✅ |

### 0.1 偏离披露 ①：工作区非「空」而是含上一 session 的 2 个交付物

- `git status --porcelain` 显示 2 个 `??`（untracked，**未修改任何已跟踪文件**）：
  `docs/v1.11.0-coldstart.md`、`docs/v1.11.0-review.md`。
- 二者是**上一 session 任务书第十四节指定的交付物**，被本 session 任务书第三节列为**必读文档**。
- **处置**：视为**预期基线的一部分**，保留并作为本 session 首个 docs commit 落盘；
  不视为「工作区脏」的实质违规。已跟踪文件 0 修改（`git diff 3ed0d9d..HEAD` 为空，HEAD 未变）。
- 分支 `v1.11.0-c2` **上一 session 已创建并当前已检出**（故不重复 `checkout -b`）。

---

## 1. 仪器规格（口径与来源）

| 项 | 内容 |
| :--- | :--- |
| 脚本 | **`tools/corpus-analysis/measure.py`**（本 session 固化进仓库；上一 session 的 `/tmp/v111/measure.py` 为其前身） |
| 口径来源 | `tools/corpus-analysis/analyze.py`（bwrap `--unshare-all` / fixture / stdin 三处口径） |
| 语料 | `~/下载/非常批处理/` 递归 `*.bat` / `*.cmd` = **151** |
| 转换口径 | `ConvertSettings(bash_check=False)`（原始转换口径，v1.8.0 起为发布口径） |
| 语法判据 | `bash_syntax_error(text) is None`；**分母 = 语法通过数** |
| 沙箱 | `bwrap --unshare-all --die-with-parent`，只读挂载 `/usr`，HOME=workdir，无网络，`timeout -k 5 30` |
| fixture / stdin | 复用 `analyze.py`：从产物提取文件名生成占位 fixture；独立 `read` 时喂空行 |
| rc==0（功能完好，严格） | `rc==0` **且** `report.todo_count==0` **且** 产物无 `# TODO` 标记（排除脚本头） |
| degraded | `rc==0` 且含 TODO |
| 崩溃判据 | `rc>=128` 且 `rc-128 ∈ {4,6,7,8,11}` |
| 输出 | `<out>/measure.json`（summary + 逐文件 rc/todo_count/markers/todos） |

**复现命令**：

```bash
python3 tools/corpus-analysis/measure.py --corpus ~/下载/非常批处理 --out /tmp/v1x
```

---

## 2. 仪器验证（三轮全量复跑，逐字段比对）

| 轮次 | 工具 | 语料 | 语法 | rc==0 | strict | degraded | TODO | 崩溃 | 超时 |
| :--- | :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| R1（上一 session） | `/tmp/v111/measure.py` | 151 | 147 | 102 | 16 | 86 | 894 | 0 | 0 |
| R2（本 session 复跑） | `/tmp/v111/measure.py` | 151 | 147 | 102 | 16 | 86 | 894 | 0 | 0 |
| R3（本 session，**仓库版**） | `tools/corpus-analysis/measure.py` | 151 | 147 | 102 | 16 | 86 | 894 | 0 | 0 |
| **v1.10.0 发布记录** | — | 151 | 147 | 102 | 16 | 86 | 894 | 0 | 0 |

**逐字段确定性**：

| 比对 | 结果 |
| :--- | :--- |
| R1 vs R2（同脚本两次独立全量运行） | **逐文件 0 差异**（`rc`/`todo_count`/`markers`/`syntax_ok`/`convert_error`） |
| R2 vs R3（`/tmp` 版 vs 仓库版） | **逐文件 0 差异** |
| 全部三轮 vs v1.10.0 记录 | **逐项一致** |

> **仪器验证结论**：仪器**可复现、确定性、与历史基线逐项吻合**，通过纪律 9 强化条款。
> 后续一切数字均出自该仪器。

**语法失败 4 例**（与路线图 §6.1 一致）：`今天访问过的程序文件所在目录.cmd`、
`史上最牛X批处理工具包09年7月11日更新版.bat`、`文件归类整理器.bat`、`运行剪贴板中的bat.bat`。

---

## 3. 前提再核（任务书 §4.2）

前一 session 的三项结论，本轮以**可复现仪器**复核，全部维持：

| 前提 | 文档声称 | **仪器实测/重算** | 结论 |
| :--- | :--- | :--- | :--- |
| **(a) 46 个全 L1/L2 文件** | `14+32 = 46`（closure §1.3，取自 `v1.9.1` §2.1「最高层」行） | 用 `v1.9.1` 表 A 逐文件 L1–L5 重算：**25**；且重算「L1 层文件数 = 0」，与 §2.1 的「L1=14」矛盾 | ❌ **不可复现**（§2.1 自身内部不一致） |
| **(b) for(10)+registry(11)+other(15)=36** | 36 单设施翻转 | `registry` **精确吻合 29/20/11**，但构成 = **26 写 + 3 导出**（`reg add`18 / `regedit /s`5 / `reg delete`2 / `regini`1 / 导出3）；项目自身归类 **L4 → 2.x**。`for` 实为 **7** 单设施文件；`other`(15) **无定义** | ❌ **不成立**（11/36 属 L4/2.x） |
| **(c) 86 → ≤41** | 达成路线图 §4 标准 3 | 可做池远小于 45（真 A 档 2 项）；乐观上限 ≤7 翻转 | ❌ **不可达** |

> 完整论证见 `docs/v1.11.0-coldstart.md` §3（上一 session）；本节为**可复现复核**。
> 归属重判见 `docs/v1.11.0-reclassification.md`；标准 3 修订见
> `docs/v1.11.0-standard-3-revision.md`。

---

## 4. 偏离披露（纪律 4）

| # | 项 | 说明 | 置信度 |
| :--- | :--- | :--- | :---: |
| 1 | 工作区含 2 个未跟踪文档 | 上一 session 交付物，非本 session 修改（§0.1） | A |
| 2 | 仪器固化进仓库 | 新增 `tools/corpus-analysis/measure.py`（纪律 9 强化要求「测量工具必须可复现」）；非产品代码路径 | A |
| 3 | 子代理不可用 | 上一 session 记录；本 session 未再尝试（避免重复失败），全程主控只读工具 | A |
| 4 | 轮次 R1 为上一 session 数据 | 本轮独立复用同一脚本重跑（R2/R3）以验证确定性，非引用 | A |

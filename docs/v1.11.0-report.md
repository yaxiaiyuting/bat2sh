# bat2sh v1.11.0 发布报告（C2 收窄执行 + 归属重判 + 标准 3 修订）

> 会话：`v1.11.0 C2 收窄执行 + 归属重判 + 标准 3 修订`。
> 交付物：分支 `v1.11.0-c2`、tag `v1.11.0` + GitHub Release、
> `docs/v1.11.0-{coldstart,review,instrument-verify,reclassification,standard-3-revision,1x-closure-final,report}.md`、
> `docs/releases/v1.11.0.md`、`tools/corpus-analysis/measure.py`。

---

## 一、总览

| 项 | 内容 |
| :--- | :--- |
| 分支名 | `v1.11.0-c2` |
| 起点 HEAD | **`3ed0d9d`**（v1.10.0 真实发布后 HEAD，含发布产物 commit） |
| 终点 HEAD | 见 §四（bump commit / pkg sync / docs） |
| tag | **`v1.11.0`** |
| 版本号裁定 | **v1.11.0**（净新增映射 **5 条** ≥ 5：`for /r`、`for /f` 字符串、`net user /delete`、`attrib +r`、`attrib -r`；另 3 条 hint 纠错） |
| 完成了什么 | C2 可做 3 翻转 + 1 质量项 + 3 hint 纠错；C2 归属重判；标准 3 修订；仪器固化 |
| 未完成什么 | 原「36 单设施翻转」（经证明 11 为 L4 写类、21 为终态/零翻转，**不可做**）；标准 1 残差 3 项（2.x 解析层） |
| **翻转计数** | **86 → 83**（strict **16 → 19**） |
| **1.x 收尾判定** | 条件 (a)(b)(c) **达成**；§4 六标准因标准 1 未达 → **不单方面宣布，提请裁定** |

---

## 二、逐条交付

### 2.1 完成的（commit 序列）

| # | commit | 内容 |
| :--- | :--- | :--- |
| 1 | `84057bf` | docs：v1.11.0 冷启动自检 + 自我审阅（前提核对：46/36/≤41 不可复现） |
| 2 | `61aa789` | tools：固化可复现语料测量仪器 + 仪器验证（纪律 9 强化） |
| 3 | `3c78de6` | docs：归属重判（registry 29/29→2.x；other 17 单设施逐条；可做=3+2） |
| 4 | `584f425` | feat：`for /r` → `find` 驱动循环（A 档，1 翻转） |
| 5 | `b0657f3` | feat：`for /f` 字符串形式 → here-string（B 档，1 翻转） |
| 6 | `8dc98da` | feat：`net user /delete` → 幂等 `userdel` + net hint 纠错（A 档，1 翻转） |
| 7 | `99ce99d` | feat：`attrib ±r` → 带守卫 `chmod ∓w`（B 档，质量项） |
| 8 | `560691a` | fix：attrib/fsutil TODO hint 文案纠错 |
| 9 | 见 §四 | docs：标准 3 修订（≤41 → ≤83） |

### 2.2 未完成的（逐条 + 原因）

| 项 | 原因 | 归属 |
| :--- | :--- | :--- |
| 原「36 单设施翻转」 | 其中 **11 = 注册表写类（L4）**、**21 = 终态/零翻转**；真可做仅 3 | 2.x / 终态（逐条见 `reclassification.md`） |
| `at`/`rasdial`/`FTP`/`WinRAR` 映射 | 置信度 **C/B**，语义不完全等价；且宿主零翻转 | 2.x（保持诚实 TODO + hint） |
| `for /r` 文件模式（`*.jpg`）/ 含空格根 / 含 `$` 字符串 | 保守拒绝（易产「看似对」的错译，纪律 1） | 保持诚实 TODO |
| 3 个语法失败（解析层） | 括号配平 / 嵌套 `if` 发射 / 引号转义 —— 均属解析层，**1.x 红线外** | **2.x**（含 2.x 启动条件） |

---

## 三、归属重判结果

| 类 | 原声称 | 重判 | 关键证据 |
| :--- | :--- | :--- | :--- |
| `registry` | 29 / 11 | **29 / 11 精确吻合，但 26 写 + 3 导出 → 全部 2.x（L4）** | 逐条 29 行表（`reclassification.md` §1.2） |
| `other` | 76 / 15 | **85 / 17 单设施：可做 1、2.x 11 文件、终态 5 文件** | 逐条 17 行 + 10 条多设施文件（§2.1/§2.2） |
| `for` | 32 / 10 | **30 / 7 单设施：可做 2、其余 2.x/终态** | §0 分桶表 |
| **合计可做** | 36 | **3 翻转 + 1 质量项** | 已全部执行 |

**无静默归 2.x**：29 + 17 + 10 + 4（语法失败）= 60 行逐条归属（纪律 16）。

---

## 四、标准 3 修订与指标

| 项 | 原 | 修订后 |
| :--- | :--- | :--- |
| 标准 3 阈值 | `degraded ≤ 41` | **`degraded ≤ 83`**（= 86 − 3 实测翻转） |
| 依据 | 路线图 §3.4（「46 文件」，**不可复现**） | 仪器实测 + 逐条归属重判 |

| 指标 | v1.10.0 | **v1.11.0** | 判定 |
| :--- | ---: | ---: | :--- |
| 语料 / 语法 | 151 / 147 | 151 / 147 | 持平 |
| rc==0 / strict / degraded | 102 / 16 / 86 | 102 / **19** / **83** | ✅ rc 持平、strict +3、degraded −3 |
| degraded TODO | 894 | 886 | −8 |
| 崩溃 / 超时 | 0 / 0 | 0 / 0 | ✅ |
| pytest | 1371 | 1405 | ✅ 不回退 |
| wine 黄金对照 | 6/6 | 6/6 | ✅ 不下降 |
| examples | 零漂移 | 零漂移 | ✅ |
| 5 张台账校验器 | 全 `[]` | 全 `[]` | ✅ |

---

## 五、1.x 收尾判定

| 条件 | v1.10.0 | v1.11.0 |
| :--- | :--- | :--- |
| (a) 剩余失败都有归属 | ✗ | **✓** |
| (b) 无可修未修 | ✗ | **✓** |
| (c) 达成（修订）标准 3 | ✗ | **✓** |

**关联披露**：路线图 §4 标准 1（语法 ≥150/151）**未达**（147/151）；4 例逐条归因为
**1 B 类源畸形（终态）+ 3 解析层（2.x）**。故**不单方面宣布「1.x 完成」**（六标准要求同时成立；
修订标准 1 超出本版授权）→ **提请裁定**（`1x-closure-final.md` §4.3）。

---

## 六、已知限制

1. 1.x 收尾**待裁定**（标准 1）。
2. **AUR 未发布**（无包、无凭据）；仓库侧 `PKGBUILD`/`.SRCINFO` 已同步 + 本地 `makepkg` 验证。
3. C2 真可做仅 3 翻转（原 36 中 11 为 L4 写类、21 为终态）。
4. `goto`/`sc`/`regsvr32`/注册表写仍为 2.x/终态。
5. 子代理不可用（`ProviderModelNotFoundError`），全程主控只读工具，无交叉复核（沿用路线图 §0.3(2) 披露）。

---

## 七、2.x 启动条件（可证伪触发器）

| 主题 | 触发条件 |
| :--- | :--- |
| C4 goto CFG | 只读 Phase-1 CFG 落盘且 151 语料逐字节 0 diff、`validate_control_flow_patterns()==[]` |
| C7 `sc` | C4 Phase-2 通过（宿主 135 解阻塞）且 ≥1 服务名→unit 有文档 evidence |
| 解析层 3 项（标准 1 残差） | A9/B5/LF-1/LF-2 同批；保持响亮失败；churn 硬上限 + examples 零漂移 |
| 注册表写（29） | 逐键配置文件映射规则 + 每条 evidence |
| B1 名称映射扩展 | 兜底后 strict 不降（≥19）且每条带 corpus evidence |

---

## 八、纪律对照

| 纪律 | 落实 |
| :--- | :--- |
| 只读诊断先行 | 冷启动/审阅/仪器验证/归属重判全部先于实现 |
| 仪器先验证 | `measure.py` 三轮逐字段一致；基线 151/147/102/16/86/894 复现 |
| 每 commit 全量 pytest | 每 commit 后 1371→1380→1390→1397→1404→1405，全绿 |
| 语义正确性与 rc==0 并列 | `for /r` find 语义、`for /f` tokens/delims/`""` 语义、`userdel` 幂等、`attrib` 只读位往返均有语义测试 |
| 测试断言验证语义 | 新增 34 条测试断言行为（bash 实跑输出/权限位），非编码现况；旧断言按新语义更新并保留意图 |
| 不发明映射 | 所有新映射均有 corpus evidence；`at`/`rasdial`/`FTP` 因置信度 C 拒绝实现 |
| 保守 TODO 优于激进转换 | `for /r` 窄形态、字符串含 `$`/反引号拒绝、`+h/+s/+a` 保持 TODO |
| 合并后必须全量验证 | 本版无合并（单分支线性 commit） |
| 主动披露偏离 | HEAD 偏差、工作区含未跟踪文档、子代理不可用、仪器重建、标准 1 未修订、不宣布收尾 |
| 锁定 HEAD | 起点 `3ed0d9d` 全程未变（无并发写入） |
| 版本号触发 | 净新增映射 **5 条**（≥5）→ 维持 `1.11.0`（判别方法见 §一） |

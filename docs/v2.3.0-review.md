# bat2sh v2.3.0 自我审阅与裁定（只读）

> 会话：v2.3.0。分支 `v2.3.0-goto-cfg`；起点 HEAD `b12e336`。
> 前置：`docs/v2.3.0-coldstart.md`、`docs/v2.3.0-reality-check.md`（**核心：逐项实测**）。
> 本文件为**只读审阅产出**（范围/风险/止损/退路/替代/验收 + 裁定）。

---

## 1. 基于实测的结论

| 项 | 实测翻转 | 判定 |
| :--- | :---: | :--- |
| goto 回跳 / 块内 | **0** | 不做（触 053 无方案） |
| **goto CFG 高级** | **0** | **「语义完整但翻转 0」** —— 唯一可翻转文件不需 CFG 高级，纯 053 阻塞 |
| B5 括号跨行 | **0** | 不做（定向探针：churn 1 语法失败文件，第二缺陷独立） |
| 注册表写逐键 | **0** | 终态（无安全映射，已结构化） |

**→ 进入任务书 §六.2「情形 B」：实测全 0 + goto CFG 高级为「语义完整但翻转 0」→ 触发 2.x 收尾评估。**
**不硬做 goto CFG 高级**（15–25 人日换翻转 0，纪律 1/17）。

---

## 2. 范围

### 2.1 做什么

| 项 | 内容 |
| :--- | :--- |
| **逐项实测** | `docs/v2.3.0-reality-check.md`（核心） |
| **2.x 收尾评估** | `docs/v2.3.0-2x-closure.md`（三条件 + goto CFG 高级归属 + 3.x 范围 + PS 解冻评估） |
| 报告与发布 | `docs/v2.3.0-report.md` + release notes + 验证日志；tag `v2.3.0` |

### 2.2 不做什么

- **不做 goto CFG 高级实现**（翻转 0 + 触 053；15–25 人日不值得）。
- **不做 B5**（实测价值 0）。
- **不做注册表写映射**（无安全映射）。
- **零转换改动**：`core/batch.py` / `rules.py` / `core/cfg.py` 等生产模块**零 diff**。

---

## 3. 风险

| 项 | 风险 | 等级 |
| :--- | :--- | :---: |
| 本版 | **零代码改动，纯文档** → 无转换风险；唯一风险是「收尾判定过宽」 | 低 |
| 收尾判定 | 若把「实测 0 但未穷尽」误判为终态 → 违反纪律 16。缓解：**逐项附实测依据 + 明确「仅 T1 真实需求触发重评」**，非无条件终态 | 中 |

---

## 4. 止损点 / 退路

| 闸门 | 阈值 | 触发动作 |
| :--- | :--- | :--- |
| 指标倒退 | 与 `b12e336` 基线不一致（syntax<149、rc0<104、strict<19、degraded>85、崩溃>0） | 停 |
| 产物漂移 | 任一文件产物变化（本版应为 0） | 停 |
| 053/A1 | 任何改动 | 停 |
| 改动模块 > 5 | — | 停 |
| HEAD 变化 | ≠ 本会话自身 commit 推进 | 停 |
| **收尾评估「仍有 ✗ 但无法归属」** | — | **暂停等用户** |

- **HEAD 回滚点 = `b12e336`**（= `main`）；全量退路 `git checkout main`。

---

## 5. 替代方案

| 方案 | 说明 | 裁定 |
| :--- | :--- | :--- |
| A 强行实现 goto CFG 高级 | 换翻转 0；触 053 | ❌ 违反纪律 1 |
| B 实现 B5 | 实测价值 0；触解析层核心 | ❌ |
| **C 只交实测报告 + 2.x 收尾评估** | 任务书情形 B | ✅ 采用 |
| D 收尾评估判定为「未达成」 | 若 (a)(b)(c) 有 ✗ | ❌ 三条件实测全 ✓（见 closure §2） |

---

## 6. 验收标准（原则性，禁止估算）

| # | 原则 | 判据 |
| :--- | :--- | :--- |
| 1 | **所有 v2.3.0 提议项有明确归属** | 逐条「不做 / 终态 / T1 重评」，无「可修但未修」 |
| 2 | **指标不倒退** | 与 `b12e336` 逐字段一致（151/149/104/19/85/897/0） |
| 3 | **零转换改动** | 产物逐字节 0 diff |
| 4 | **wine 不下降** | 6/6 |
| 5 | **053/A1 未触** | 零改动 |
| 6 | **2.x 收尾三条件逐条判定** | (a)(b)(c) 均有实测依据，无「无归属」项（纪律 16） |
| 7 | **goto CFG 高级归属明确** | 「语义完整但翻转 0」→ **终态（仅 T1 重评）**，非静默归 3.x |
| 8 | **examples 零漂移** | tracked 对零字节 |
| 9 | **pytest 只增不减，0 failed** | ≥1448 |

---

## 7. 裁定

| 子项 | 裁定 | 理由（实测） |
| :--- | :--- | :--- |
| goto 回跳 / 块内 | **不做 → 终态（T1 重评）** | 触 053；翻转 0–1 |
| **goto CFG 高级** | **不做 → 终态（T1 重评）** | **语义完整但翻转 0**；唯一可翻转文件不需它 |
| B5 | **不做 → 终态/解析层重写** | 定向探针价值 0；多缺陷 |
| 注册表写 | **终态（维持）** | 无安全映射；已结构化 |
| **2.x 收尾评估** | **触发并执行** | 三条件实测全 ✓ |
| 版本 | **`2.3.0`**（minor，含收尾声明） | 收尾为战略里程碑（沿用 v1.11.0 收尾版本为 minor 先例）；无代码改动亦如实披露 `2.2.1` 为可选 |

**总判定：实测全 0 + goto CFG 高级「语义完整但翻转 0」→ 触发 2.x 收尾评估；本版零代码改动，
交付逐项实测 + 收尾声明 + 报告。**

---

## 8. 实现后回填

（本版零代码改动；最终指标与逐文件 diff 见 `docs/v2.3.0-report.md` §3 与 release notes §4。）

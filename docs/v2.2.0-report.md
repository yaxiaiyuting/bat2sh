# bat2sh v2.2.0 报告（实测优先：goto 高级评估 + sc 诚实 TODO 结构化）

> 交付：`docs/v2.2.0-coldstart.md`、`docs/v2.2.0-reality-check.md`（**核心产出**）、
> `docs/v2.2.0-review.md`、`docs/releases/v2.2.0.md`、本文件；
> 代码 `core/batch.py`（`cmd_sc`）+ `core/rules.py` + `tests/test_batch_sc.py`。

| 项 | 内容 |
| :--- | :--- |
| **分支名** | `v2.2.0-goto-adv` |
| **起点 HEAD** | `318d543`（= v2.1.0 终点；= `main` = `origin/main`） |
| **代码/文档终点 HEAD** | `48d9903`（docs 回填 commit） |
| **tag** | `v2.2.0` |
| **发布终点 HEAD（= tag 目标）** | `3b318ba`（bump commit） |
| **tag 后 pkg sync HEAD** | `0f68006`（PKGBUILD/.SRCINFO） |

---

## 1. 逐项实测结果（roadmap 估算 vs 实测）

| 项 | roadmap 估算 | **实测翻转** | 关键事实 | 裁定 |
| :--- | :--- | :---: | :--- | :--- |
| goto 回跳 | 0–2 | **0**（候选 1） | `446/28`；候选文件须触 `_label_line`（053）+ CFG 高级 | 退回 v2.3+ |
| goto 块内 | 0–2 | **0** | `457/8`；无 rc==0 单桶；053 同域 | 退回 v2.3+ |
| P5 | 0 | **0** | `redundant 27/5`、`forward_skip 551/26`；无单桶宿主 | 不做 |
| `sc` | 0 | **0** | rc==0 文件 171；无单桶；94 服务名无 Linux unit | **实现诚实 TODO 结构化** |
| 注册表写 | 「11 可能翻转」 | **0** | 11 单设施但 Linux 无注册表；已结构化 `# TODO[REG]` | 维持/终态 |
| B5 | ? | **0** | `合并文本`×3 需 `for/f` 变量选项 + 循环体 goto 双解锁 | 退回 v2.3+ |

**实测翻转总数 = 0。** 按任务书「全部翻转 0 → 只交报告 + 诚实 TODO 化」执行：
唯一可做项 = `sc` 结构化诚实 TODO。

---

## 2. 完成了什么（逐条）

1. **冷启动自检**（`v2.2.0-coldstart.md`）：前置五条全过；仪器复现；复述 v2.2 scope + 实测优先协议；
   前提核对（P5 无候选 / sc 0 翻转 / 注册表维持 / B5 退回）；偏离披露。
2. **逐项实测（核心）**（`v2.2.0-reality-check.md`）：对 6 项 roadmap 提议逐项给出
   TODO 数 / 可翻转文件数 / 宿主集中度 / 是否需 053 / 是否需 CFG 高级 + 裁定；六次估算错误统计。
3. **自我审阅与裁定**（`v2.2.0-review.md`）：范围/风险/最小复现/止损/退路/替代/原则性验收；
   裁定 = 实现 `sc` 结构化诚实 TODO；P5/B5/goto 高级/注册表映射显式归 v2.3+ 或终态。
4. **`sc` 诚实 TODO 结构化**（commit `20ba4b1`）：`cmd_sc` + `rules.py` 路由；零映射；安全回退。
5. **回归测试**（commit `865eb46`）：`tests/test_batch_sc.py` 12 条（不依赖外部语料）。
6. **文档**（commit `9fd9174` + 本批）：coldstart / reality-check / review / release notes / 本报告。
7. **发布**：preflight 通过（1438 passed/10 skipped）→ FF 合并 `main` → bump `3b318ba` → tag `v2.2.0` →
   push → **tag 上 CI 全绿**（run `35252605210`，3.12/3.13/3.14）+ main 两次全绿 →
   tarball sha256 `bb9e62d5…` 同步 `PKGBUILD` + `makepkg --printsrcinfo` 重生成 `.SRCINFO`（pkg sync `0f68006`）→
   `makepkg -f` 构建 `bat2sh 2.2.0-1` → GitHub Release **Latest** → 本机安装验证
   （`docs/releases/v2.2.0-verification.md`）。AUR 不进。

---

## 3. 实测翻转数（引用仪器）

**0 文件翻转。**

- 仪器：`tools/corpus-analysis/measure.py`；基线（`318d543`）vs 本版 summary 逐字段一致
  （151/149/104/19/85/897/0）。
- 转换产物 diff：**仅 1 文件**（`系统优化.bat`，169 行，纯 `# TODO[SC]` tag 插入；原命令逐字未变）。
- 其余 150 文件产物逐字节一致。

---

## 4. 未完成什么（逐条 + 原因）

| 未完成项 | 原因（实测） | 归属 |
| :--- | :--- | :--- |
| goto 回跳 → 循环 | 须在标签处发射 `while`（触 `_label_line`=053）+ 需 CFG 高级；翻转仅 1 | v2.3+ |
| goto 块内 | 053 同域；翻转 0 | v2.3+ |
| P5 | 实测 0 | 不做 |
| 注册表写映射 | Linux 无注册表；映射=语义近似；已结构化 | 终态 |
| B5 | 需 `for/f` 变量选项 + 循环体 goto 双解锁 | v2.3+ |
| `service` 名称表 | 91 条全 `form=none`，零收益 | 不做 |

---

## 5. 是否触及 053 / A1

**否。** `git diff 318d543..HEAD` 仅：
- `core/batch.py`：**新增** `cmd_sc` / `_parse_sc_invocation` / `_join_sc_params`（不改既有方法）；
- `core/rules.py`：`"sc": "cmd_todo_hint"` → `"cmd_sc"` 单表项；
- 新增 `tests/test_batch_sc.py`。

`_prepass`/`_label_line`/`_convert_for`/`_emit_for_f`/`_convert_if`/`_pop_block`/`_close_block_line`/`_Block`/`await_paren_close`
与 `_expand_vars`/`env_repl`/`_a1_*`/`_guard_unset_variable_refs` **零改动**（对照 v2.0.0/v2.1.0 硬约束）。

---

## 6. 指标对比

| 指标 | v2.1.0 | **v2.2.0** | 判定 |
| :--- | ---: | ---: | :--- |
| 语料 / 语法通过 | 151 / 149 | 151 / **149** | ✅ 持平 |
| rc0 / strict | 104 / 19 | **104 / 19** | ✅ 持平 |
| degraded / TODO | 85 / 897 | **85 / 897** | ✅ 持平 |
| 崩溃 / 超时 | 0 / 0 | **0 / 0** | ✅ |
| pytest | 1436 | **1448** | ✅ +12 |
| wine 黄金对照 | 6/6 | **6/6** | ✅ |
| examples tracked 对漂移 | 0 | **0** | ✅ |
| 转换产物 diff | — | **1 文件 / 169 行**（纯 sc tag） | ✅ 预期内 |

---

## 7. 下一版起点（v2.3.0）

| 项 | 值 |
| :--- | :--- |
| **v2.3.0 起点 HEAD** | 本回填 commit 的 HEAD（v2.2.0 tag 后 docs 回填；以 `git rev-parse HEAD` 为准）；tag `v2.2.0` = `3b318ba` |
| 建议分支名 | `v2.3.0-goto-cfg` |
| 前置设施 | `core/cfg.py`（CFG 数据模型）+ `tools/cfg`；C4 台账；wine harness 6/6 |
| 范围建议 | goto CFG 高级（回跳→循环 / 块内 goto，先解 053 安全发射）+ B5；注册表写逐键评估 |

---

## 8. 已知限制

见 `docs/releases/v2.2.0.md` §6（goto 高级/B5/注册表写未实现且均有实测依据；service 名称表未落；
CFG 为括号净值近似；LF-1 未修）。

---

> **本报告为 v2.2.0 最终产出。**（HEAD/tag/安装验证待回填。）

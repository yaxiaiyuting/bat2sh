# bat2sh v1.9.0 本机安装验证日志

> 环境：CachyOS / Arch Linux（本机 `~/.local` 免打包安装，`./install.sh`）。
> 发布物：tag `v1.9.0` → commit `dce22a8`；打包同步 commit `e056f71`。
> 语言：Linux x86_64；测试矩阵 Python 3.12 / 3.13 / 3.14（CI）。

---

## 1. 版本

```
$ bat2sh --version
bat2sh 1.9.0
$ grep __version__ ~/.local/lib/bat2sh/bat2sh/__init__.py
6:__version__ = "1.9.0"
```

安装：`./install.sh` → 模块 `~/.local/lib/bat2sh`、启动器 `~/.local/bin/bat2sh`、桌面项与图标，
输出「完成」。安装前为 `1.8.3`，安装后为 `1.9.0`。

---

## 2. 实跑（5 项）

### 2.1 053 块栈修复（安装版 CLI）

输入 `v190_verify.bat`：

```bat
@echo off
for %%i in (a b) do if %%i==a (echo A ) else (if %%i==b (echo B ) else (
echo body1
echo body2
))
echo after
```

产物（**else 体正确进入循环**，无「多余的 ')'」）：

```bash
for i in a b; do
    if [ "${i}" = "a" ]; then
        echo "A"
    else
        if [ "${i}" = "b" ]; then
            echo "B"
        else
            echo "body1"
            echo "body2"
        fi
    fi
done
echo "after"
```

- `bash -n v190_verify.sh` → **OK**
- `bash v190_verify.sh` → `A` / `B` / `after`（else 分支按语义不执行，正确）

### 2.2 内容丢失修复

输入 `Wmic ComputerSystem Where "Name='Administrator'" Call ReName "X"`：

产物含 `# TODO: 手动检查: Wmic …`（**v1.8.3 该行整条蒸发**）。

### 2.3 映射扩展

输入 `tskill logo_1` → 产物 `# TODO: 手动检查: tskill logo_1`（C 档：建议 + TODO）。

### 2.4 打包元数据

```
PKGBUILD  sha256sums = 93b0da08975a610b74fc19407251103c7219258644724bc5ccd28826398e8337
.SRCINFO  pkgver = 1.9.0；source 行由 makepkg --printsrcinfo 生成（未硬编码）
```
sha256 取自 `https://github.com/yaxiaiyuting/bat2sh/archive/refs/tags/v1.9.0.tar.gz`
（与 PKGBUILD `source` 完全一致）。

### 2.5 发布页

`https://github.com/yaxiaiyuting/bat2sh/releases/tag/v1.9.0` — 可访问，
正文含「口径说明」小节（rc==0 严格定义 + 预期下降说明）。

---

## 3. 严格口径门槛（151 语料）

| 指标 | v1.8.3 | **v1.9.0** | 门槛 |
| :--- | ---: | ---: | :--- |
| 语法通过率（分母 151） | 96.0% | **97.4%（147/151）** | ≥93% ✅ |
| rc==0（原始） | 102 | **102** | — |
| rc==0（功能完好，严格） | 22 | **16** | 见「口径说明」 |
| degraded（含 TODO） | 80 | **86** | 单列 |
| rc≠0 | 43 | **45** | — |
| env_dep | 16 | **15** | 单列 |
| 崩溃类 | 0 | **0** | **=0 ✅** |
| examples 零字节漂移 | ✅ | **✅（3/3）** | ✅ |
| 丢失检测误报 | — | **0/151** | — |

> 「功能完好 22 → 16」为**诚实化**（把 6 条「假 rc==0」纠正为真 degraded），
> 逐条证据见 `docs/v1.9.0-rounds.md`；release notes 已预先说明，避免误读。

---

## 4. 测试与 CI 时序

| 阶段 | 结果 |
| :--- | :--- |
| 全量 pytest（本机） | **1244 passed** |
| tag 前 `release-preflight.sh`（空 HOME 类 CI） | **1242 passed, 2 skipped**；工作区干净；通过 |
| tag 前全量语料复跑 | 见上表 |
| tag `v1.9.0` → `dce22a8` | ✅ tag 打在 bump commit 上 |
| tag 上 CI（Python 3.12 / 3.13 / 3.14） | **3/3 全绿**（run `35115085074`；workflow 仅 `push:[main]` 触发，tag 指向同一 commit，故按该 commit 的运行判定） |
| 打包同步 commit `e056f71` | ✅ 已推送 |

**tag 后 CI 确认方式说明**：`.github/workflows/test.yml` 的触发条件为 `push: branches: [main]`，
tag 本身不触发新 run。因此「tag 上 CI 全绿」= **tag 所指向 commit（`dce22a8`）的 main push run 全绿**。
本次已确认该 run 的 3 个矩阵 job 均为 `success`。CI 红则立即停止（本版未触发）。

---

## 5. 覆盖的验收标准（任务书第六节）

| # | 标准 | 状态 |
| :--- | :--- | :--- |
| 1 | pytest 全绿、无回退 | ✅ 1222→1244 |
| 2 | 内容丢失已修（013/023/079/001 无静默丢弃） | ✅ 001 的 135 条 attrib 全部落盘（134 TODO + 1 未知命令）；检测误报 0/151 |
| 3 | 053 已修，rc 变化 3 条逐条说明 | ✅ 见 rounds（均 sandbox/empty-input artifact） |
| 4 | 映射新增 10 条，`validate_mappings()` 通过 | ✅ 37 条，0 违规，evidence 逐条复核 |
| 5 | B-1/B-2/B-3 按光谱判定并落盘 | ✅ rounds §P4（B-1/B-2 修；B-3 记录，`title` 偏离已披露） |
| 6 | 151 语料严格口径实测 | ✅ 本文件 §3 |
| 7 | examples 零字节漂移 | ✅ 3/3 |
| 8 | CI 3.12/3.13/3.14 全绿、tag 上确认 | ✅ §4 |
| 9 | Release 页可访问 + `--version` 正确 | ✅ §1/§2.5 |

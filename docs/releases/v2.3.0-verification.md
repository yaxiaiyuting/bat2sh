# bat2sh v2.3.0 本机安装验证日志

> 会话：v2.3.0（goto CFG 高级实测 + 2.x 收尾）。**本版零代码改动。**
> 本文件记录 **tag 之后**的本机验证（纪律 12：tag 后仍须实跑验证）。

---

## 1. 环境

| 项 | 值 |
| :--- | :--- |
| 平台 | CachyOS / Arch Linux（bash 5.x；`makepkg` 可用） |
| Python | 3.14.7（本机；CI 覆盖 3.12 / 3.13 / 3.14） |
| HEAD（main，pkg sync） | `c1d7aae` |
| tag | `v2.3.0` @ `df2b459`（bump commit） |

---

## 2. 版本号（3 处）

| 来源 | 输出 |
| :--- | :--- |
| 源码 `PYTHONPATH=python python3 -m bat2sh --version` | `bat2sh 2.3.0` |
| 开发启动器 `./scripts/bat2sh-dev --version` | `bat2sh 2.3.0` |
| `pyproject.toml` / `python/bat2sh/__init__.py` | `2.3.0` / `2.3.0` |

---

## 3. CI（tag 上 + main，关键门槛）

| 项 | 值 |
| :--- | :--- |
| run id（tag `v2.3.0`） | `35347305954` ✅ **success**（3.12 / 3.13 / 3.14） |
| run id（main @ bump `df2b459`） | `35347302483` ✅ success |
| run id（main @ pkg sync `c1d7aae`） | `35347511967` ✅ success |
| `release-preflight.sh` | ✅ 通过（1438 passed / 10 skipped，空 HOME 类 CI） |

---

## 4. 打包（PKGBUILD 本地验证）

```text
makepkg --printsrcinfo > .SRCINFO        # .SRCINFO 由 PKGBUILD 重生成
makepkg -f --noconfirm                   # 实际构建
→ 完成创建：bat2sh 2.3.0-1
```

| 项 | 值 |
| :--- | :--- |
| tarball sha256 | `fd34bbda2e71645bdbb757087804eae2e71b7a4cb96ee961b6e75b6d2261c876` |
| `PKGBUILD` `pkgver` / `sha256sums` | `2.3.0` / 同上（已同步） |
| `.SRCINFO` | `pkgver = 2.3.0`、`source`/`sha256sums` 一致 |
| 产包 | `bat2sh-2.3.0-1-any.pkg.tar.zst`（214712 bytes） |
| `makepkg` source sha256 校验 | **通过** |

**AUR**：本版**不推送**（同 2.x 前序版本）。

---

## 5. 零代码改动实证

| 检查 | 结果 |
| :--- | :--- |
| `git diff --stat b12e336..v2.3.0 -- python/` | 仅 `python/bat2sh/__init__.py`（版本号 1 行） |
| 生产转换模块 | `core/batch.py` / `rules.py` / `core/cfg.py` 等 **零 diff** |
| 151 语料产物 | **逐字节 0 diff**（`script.sh` 聚合 sha256 一致） |
| 053 / A1 路径 | **未触** |

---

## 6. 语料指标（tag 后复核）

| 指标 | v2.2.0 | **v2.3.0** |
| :--- | ---: | ---: |
| 语法通过 / 失败 | 149 / 2 | **149 / 2** |
| rc==0 | 104 | **104** |
| strict | 19 | **19** |
| degraded | 85 | **85** |
| degraded TODO | 897 | **897** |
| 崩溃 / 超时 | 0 / 0 | **0 / 0** |
| pytest | 1448 | **1448** |
| wine | 6/6 | **6/6** |
| examples tracked 对 | 零漂移 | **零漂移** |
| 转换产物 diff | — | **0 文件** |

---

## 7. 2.x 收尾（本版核心）

- `docs/v2.3.0-2x-closure.md`：**2.x 收尾成立**（三条件全 ✓），冻结维护模式。
- `goto CFG 高级` → **终态（仅 T1 真实需求重评）**。
- 3.x 唯一大方向 = **PS 解冻**（需求驱动，未触发 T3）。

---

## 8. GitHub Release

| 项 | 值 |
| :--- | :--- |
| URL | https://github.com/yaxiaiyuting/bat2sh/releases/tag/v2.3.0 |
| 状态 | **Latest** |
| notes | `docs/releases/v2.3.0.md` |

---

> **本日志为 v2.3.0 tag 后的本机验证记录。**

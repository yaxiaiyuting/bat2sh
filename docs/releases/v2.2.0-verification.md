# bat2sh v2.2.0 本机安装验证日志

> 会话：v2.2.0（实测优先：goto 高级评估 + sc 结构化诚实 TODO）。
> 本文件记录 **tag 之后**的本机验证（纪律 12：tag 后仍须实跑验证）。

---

## 1. 环境

| 项 | 值 |
| :--- | :--- |
| 平台 | CachyOS / Arch Linux（bash 5.x；`makepkg` 可用） |
| Python | 3.14.7（本机；CI 覆盖 3.12 / 3.13 / 3.14） |
| HEAD（main，pkg sync） | `0f68006` |
| tag | `v2.2.0` @ `3b318ba`（bump commit） |

---

## 2. 版本号（3 处）

| 来源 | 输出 |
| :--- | :--- |
| 源码 `PYTHONPATH=python python3 -m bat2sh --version` | `bat2sh 2.2.0` |
| 开发启动器 `./scripts/bat2sh-dev --version` | `bat2sh 2.2.0` |
| `pyproject.toml` / `python/bat2sh/__init__.py` | `2.2.0` / `2.2.0` |

---

## 3. CI（tag 上 + main，关键门槛）

| 项 | 值 |
| :--- | :--- |
| run id（tag `v2.2.0`） | `35252605210` ✅ **success**（3.12 / 3.13 / 3.14） |
| run id（main @ bump `3b318ba`） | `35252601387` ✅ success |
| run id（main @ pkg sync `0f68006`） | `35252771336` ✅ success |
| `release-preflight.sh` | ✅ 通过（1438 passed / 10 skipped，空 HOME 类 CI） |

---

## 4. 打包（PKGBUILD 本地验证）

```text
makepkg --printsrcinfo > .SRCINFO        # .SRCINFO 由 PKGBUILD 重生成
makepkg -f --noconfirm                   # 实际构建
→ 完成创建：bat2sh 2.2.0-1
```

| 项 | 值 |
| :--- | :--- |
| tarball sha256 | `bb9e62d5286fdd08f50a25a9a21719dd8e8ee611349bfab2d2ed59c3bd52a69e` |
| `PKGBUILD` `pkgver` / `sha256sums` | `2.2.0` / 同上（已同步） |
| `.SRCINFO` | `pkgver = 2.2.0`、`source`/`sha256sums` 一致 |
| 产包 | `bat2sh-2.2.0-1-any.pkg.tar.zst`（214741 bytes） |
| `makepkg` source sha256 校验 | **通过** |

**AUR**：本版**不推送**（同 v2.0.0/v2.1.0）→ 仅仓库侧同步 + 本地 `makepkg` 验证。

---

## 5. `sc` 结构化诚实 TODO 实跑

源（`/tmp/bat2sh-v220-verify/verify.bat`）：

```bat
@echo off
sc config Spooler start= auto
sc query state= all
sc stop Spooler
```

命令与结果：

```text
$ PYTHONPATH=python python3 -m bat2sh --cli verify.bat -o verify.sh
[已写出] verify.bat -> verify.sh | 转换 0 行 | 错误 0 | 警告 0 | TODO 3
$ bash -n verify.sh && echo SYNTAX OK
SYNTAX OK
```

产物（`# TODO[SC]` 结构化；**无 systemctl 命令**，零映射）：

```bash
# TODO[SC] op=config service="Spooler" params="start=auto": 手动检查: sc config Spooler start= auto
# TODO[SC] op=query params="state=all": 手动检查: sc query state= all
# TODO[SC] op=stop service="Spooler": 手动检查: sc stop Spooler
```

`--fix-todos` 扫描器兼容性（实测）：

```text
out_line=6 category=service original='sc config Spooler start= auto'
out_line=7 category=service original='sc query state= all'
out_line=8 category=service original='sc stop Spooler'
```

`core/cfg.py`（v2.1.0 设施）仍可用（形态 7/7 与 C4 台账一致；151 语料 `validate_cfg` 0 问题）：
见 `tools/cfg/cfg_report.py`（只读，未变）。

---

## 6. 语料指标（tag 后复核）

| 指标 | v2.1.0 | **v2.2.0** |
| :--- | ---: | ---: |
| 语法通过 / 失败 | 149 / 2 | **149 / 2** |
| rc==0 | 104 | **104** |
| strict | 19 | **19** |
| degraded | 85 | **85** |
| degraded TODO | 897 | **897** |
| 崩溃 / 超时 | 0 / 0 | **0 / 0** |
| pytest | 1436 | **1448** |
| wine | 6/6 | **6/6** |
| examples tracked 对 | 零漂移 | **零漂移** |
| 转换产物 diff | — | **1 文件 / 169 行**（`系统优化.bat`，纯 `# TODO[SC]` tag） |

---

## 7. GitHub Release

| 项 | 值 |
| :--- | :--- |
| URL | https://github.com/yaxiaiyuting/bat2sh/releases/tag/v2.2.0 |
| 状态 | **Latest** |
| notes | `docs/releases/v2.2.0.md` |

---

> **本日志为 v2.2.0 tag 后的本机验证记录。**

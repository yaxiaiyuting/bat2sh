# bat2sh v1.10.0rc1 本机安装验证日志（pre-release）

> 会话：`session-lex`（C(lex) 词法层残余额账）。tag `v1.10.0rc1` 打在 `20b1585`。

---

## 1. 版本与时序

| 项 | 值 |
| :--- | :--- |
| 版本号（`pyproject.toml` / `__init__.py`） | `1.10.0rc1` |
| bump commit（tag 目标） | `20b1585`（`chore: bump version to v1.10.0rc1`） |
| tag | `v1.10.0rc1`（annotated） |
| 分支 | `session-lex`（已 push） |
| GitHub Release | **pre-release**（`isPrerelease:true`）；`Latest` 仍为 v1.9.2 |
| 发布说明 | `docs/releases/v1.10.0rc1.md` |
| `release-preflight.sh` | ✅ 通过（空 HOME 类 CI 环境：**1361 passed, 10 skipped**） |

## 2. CI（tag 上）

| run | 事件 | head | 结果 |
| :--- | :--- | :--- | :--- |
| `35225646708` | `push` tag `v1.10.0rc1` | `20b1585` | ✅ **success**（3.12 / 3.13 / 3.14，1m10s） |
| `35225642925` | `push` 分支 `session-lex` | `20b1585` | ✅ success（1m34s） |

## 3. 本机实跑（6 项）

| # | 项 | 命令 | 结果 |
| :--- | :--- | :--- | :--- |
| 1 | 源码直跑版本 | `./scripts/bat2sh-dev --version` | `bat2sh 1.10.0rc1` |
| 2 | 模块入口版本 | `PYTHONPATH=python python3 -m bat2sh --version` | `bat2sh 1.10.0rc1` |
| 3 | 免打包安装 | `PREFIX=/tmp/lex/prefix ./install.sh` | 完成（模块/启动器/desktop/图标） |
| 4 | 安装版启动器版本 | `/tmp/lex/prefix/bin/bat2sh --version` | `bat2sh 1.10.0rc1` |
| 5 | 安装版转换（词法层残余：LF-3 诚实 TODO） | `/tmp/lex/prefix/bin/bat2sh --cli demo_lex.bat --print` | 见下，`bash -n` 通过 |
| 6 | 词法层残余只读报告 | `python3 tools/lex/lexical_report.py --no-corpus` | 台账 OK；4 条 trigger 均 `REPRODUCES` |

输入 `demo_lex.bat`（LF-3 最小复现）：

```bat
@echo off
set m3=987654
set m=abcdefg
set /a m3=%m3%%m:~0,1%%%%~1
echo done
```

产物（节选，`bash -n` 通过）：

```bash
set -euo pipefail

m3="987654"
m="abcdefg"
# TODO: 手动检查: set /a m3=%m3%%m:~0,1%%%%~1
echo "done"
```

> 说明：LF-3 未被伪修复——转换器维持**诚实 TODO**（`bash -n` 通过、不产出坏 bash），
> 与 `docs/session-lex-review.md`「保守 TODO 优于激进转换」一致。

## 4. 指标快照（本会话，`/tmp/lex/measure.py`）

| 指标 | 值 |
| :--- | ---: |
| 语料 | 151 |
| 语法通过 | **147/151** |
| rc==0（原始） | **102** |
| 功能完好（严格） | **16** |
| degraded | **86**（TODO 894） |
| 崩溃 / 超时 | 0 / 0 |
| 转换产物逐字节 diff（vs `68dfbdd`） | **0 文件** |
| examples tracked 漂移 | **0**（3/3） |
| wine 黄金对照 | **6/6 MATCH** |
| pytest | **1371 passed** |

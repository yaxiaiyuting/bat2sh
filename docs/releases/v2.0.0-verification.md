# bat2sh v2.0.0 本机安装验证日志

> 会话：v2.0.0 解析层/词法层硬化。
> 本文件记录 **tag 之后**的本机验证（纪律 12「pre-release 也是发布」的精神：tag 后仍须实跑验证）。

---

## 1. 环境

| 项 | 值 |
| :--- | :--- |
| 平台 | CachyOS / Arch Linux（bash 5.x；`makepkg` 可用） |
| Python | 3.14.7（本机；CI 覆盖 3.12 / 3.13 / 3.14） |
| HEAD（main） | `a227908`（pkg sync commit） |
| tag | `v2.0.0` @ `26e3e08`（bump commit） |

---

## 2. 版本号（3 处）

| 来源 | 输出 |
| :--- | :--- |
| 源码 `PYTHONPATH=python python3 -m bat2sh --version` | `bat2sh 2.0.0` |
| 开发启动器 `./scripts/bat2sh-dev --version` | `bat2sh 2.0.0` |
| `pyproject.toml` / `python/bat2sh/__init__.py` | `2.0.0` / `2.0.0` |

---

## 3. CI（tag 上，关键门槛）

| 项 | 值 |
| :--- | :--- |
| run id（tag `v2.0.0`） | `35245274500` |
| 结果 | ✅ **success**（Python 3.12 / 3.13 / 3.14 三矩阵全绿） |
| 同 commit 的 main run | `35245271983` ✅ success |

---

## 4. 打包（PKGBUILD 本地验证）

```text
makepkg --printsrcinfo > .SRCINFO        # .SRCINFO 由 PKGBUILD 重生成
makepkg -f --noconfirm                   # 实际构建
→ 完成创建：bat2sh 2.0.0-1
```

| 项 | 值 |
| :--- | :--- |
| tarball sha256 | `3ce938245667befe9185f1147a08c59a61b665e9d3576253731dc7b297fa8d6d` |
| `PKGBUILD` `pkgver` / `sha256sums` | `2.0.0` / 同上（已同步） |
| `.SRCINFO` | `pkgver = 2.0.0`、`source`/`sha256sums` 一致 |
| 产包 | `bat2sh-2.0.0-1-any.pkg.tar.zst`（210460 bytes） |
| `.PKGINFO` | `pkgname = bat2sh`、`pkgver = 2.0.0-1` |
| `makepkg` source sha256 校验 | **通过**（日志「bat2sh-2.0.0.tar.gz ... 通过」） |

**AUR**：本版**不推送**（AUR 上无 `bat2sh` 包、本机无 AUR SSH 凭据）→ 仅仓库侧同步 + 本地 `makepkg` 验证。

---

## 5. 转换实跑（覆盖 P1 + P2）

源（`/tmp/opencode/v200verify/verify.bat`）：

```bat
@echo off
set "p=C:\a"
if exist "%p:"=%" (echo yes)
for /f "delims=" %%i in ('dir /s/b/a/ad') do (
  dir /ta/a "%%i"\*.exe 2>nul|findstr /r "^%today%" && (
    echo a >>list.htm
    echo b >>list.htm))
start list.htm
```

命令与结果：

```text
$ ./scripts/bat2sh-dev --cli verify.bat -o verify.sh
[已写出] verify.bat -> verify.sh | 转换 5 行 | 错误 0 | 警告 3 | TODO 1
$ bash -n verify.sh && echo SYNTAX OK
SYNTAX OK
```

产物关键行（证明两处修复）：

```bash
if [ -e "${p//\"/}" ]; then        # ← P2：%p:"=% 引号剥离
...
while IFS= read -r i _; do
    # TODO: 复杂管道需手动重写
    ...
    # echo a >>list.htm
    # echo b >>list.htm             # ← P1：降级管道组体注释，括号配平
done < <(ls -1R)
nohup list.htm >/dev/null 2>&1 &   # ← 循环外，块结构正确
```

**结论**：P1（括号配平）与 P2（引号剥离）均端到端生效，产物 `bash -n` 通过。

---

## 6. 语料指标（tag 后复核）

| 指标 | v1.11.0 | **v2.0.0** |
| :--- | ---: | ---: |
| 语法通过 / 失败 | 147 / 4 | **149 / 2** |
| rc==0 | 102 | **104** |
| strict | 19 | **19** |
| degraded | 83 | 85（定义性位移） |
| 崩溃 / 超时 | 0 / 0 | **0 / 0** |
| pytest | 1405 | **1412** |
| wine | 6/6 | **6/6** |

---

> **本日志为 v2.0.0 tag 后的本机验证记录。**

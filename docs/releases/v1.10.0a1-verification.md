# bat2sh v1.10.0a1 本机安装验证日志（pre-release）

> 会话：`session-b1b2`（B1 名称映射 + B2 输出契约）。tag `v1.10.0a1` 打在 `90af605`。

---

## 1. 版本与时序

| 项 | 值 |
| :--- | :--- |
| 版本号（`pyproject.toml` / `__init__.py`） | `1.10.0a1` |
| bump commit（tag 目标） | `90af605`（`chore: bump version to v1.10.0a1`） |
| tag | `v1.10.0a1`（annotated） |
| 分支 | `session-b1b2`（已 push） |
| GitHub Release | **pre-release**（`isPrerelease:true`、`isDraft:false`）；`Latest` 仍为 v1.9.2 |
| 发布说明 | `docs/releases/v1.10.0a1.md` |

## 2. 本机实跑（5 项）

| # | 项 | 命令 | 结果 |
| :--- | :--- | :--- | :--- |
| 1 | 源码直跑版本 | `./scripts/bat2sh-dev --version` | `bat2sh 1.10.0a1` |
| 2 | 模块入口版本 | `PYTHONPATH=python python -m bat2sh --version` | `bat2sh 1.10.0a1` |
| 3 | 免打包安装 | `PREFIX=/tmp/b1b2/prefix ./install.sh` | 完成（模块/启动器/desktop/图标） |
| 4 | 安装版启动器版本 | `/tmp/b1b2/prefix/bin/bat2sh --version` | `bat2sh 1.10.0a1` |
| 5 | 安装版转换（B1+B2 特性） | `/tmp/b1b2/prefix/bin/bat2sh --cli pf.bat --print` | 见下 |

输入 `pf.bat`：

```bat
@echo off
echo %ProgramFiles%\App
del /f /q "%ALLUSERSPROFILE%\Documents\DrWatson\*.*" >nul 2>nul
dxdiag /t out.txt
perfmon /report
ipconfig /all
```

产物（节选，`bash -n` 通过）：

```bash
set -euo pipefail

# TODO: 手动检查: echo %ProgramFiles%\App
rm -f "/usr/share/Documents/DrWatson"/*.* >/dev/null 2>/dev/null
# TODO: 手动检查: dxdiag /t out.txt
# TODO: 手动检查: perfmon /report
ip addr show
```

统计：转换 5 行 / 错误 0 / 警告 2 / TODO 3。

- `%ProgramFiles%` → **B1 行级诚实 TODO**（原为静默 `${ProgramFiles:-}`）；
- `%ALLUSERSPROFILE%` → **B1 映射** `/usr/share`（+ 警告）；
- 裸 `dxdiag` / `perfmon` → **B2 结构化诚实 TODO**（原为透传 → command not found）；
- `ipconfig /all` → `ip addr show`（既有行为，不变）。

## 3. 回归与门槛

| 项 | 值 |
| :--- | :--- |
| pytest（本机） | **1344 passed**（v1.9.2 基线 1262） |
| `release-preflight.sh`（类 CI 空 HOME） | 通过（1334 passed / 10 skipped） |
| wine 黄金对照 harness | **6/6 MATCH**（g01–g06；g05 仍 MATCH，A1 未回退） |
| 151 语料 | 语法 147/151、rc==0 102、功能完好 **16**、degraded 86、崩溃/超时 0 |
| 151 语料产物 diff vs v1.9.2 | 仅 3 文件（031 / 058 / 077），全部预期 |
| examples tracked 对 | 3/3 零字节漂移 |

## 4. CI 时序（tag 上）

| 时间（UTC） | 事件 |
| :--- | :--- |
| 17:16 前后 | 分支 `session-b1b2` + tag `v1.10.0a1` push |
| 17:18:32 | 触发 PR #3（`session-b1b2` → main）的 `pull_request` 工作流（run `35127369625`） |
| 17:19:36 / 17:19:42 / 17:19:46 | **test (3.12) / (3.13) / (3.14) 全部 pass** |
| 17:20:08 | GitHub Release `v1.10.0a1` 标记 pre-release |

> **说明（主动披露）**：仓库 CI 仅触发 `push main` 与 `pull_request`（见 `.github/workflows/test.yml`），
> **分支/tag push 不触发**。为满足「tag 上 CI 全绿」的发布纪律，本 session 开了**草稿 PR #3**
> （`session-b1b2` → main）作为 CI 入口与审阅入口，**未合并 main**。
> PR head = tag commit = `90af605`。若不需要该 PR，可直接关闭（不影响 tag 与 release）。

## 5. 包元数据（如需后续 AUR 同步）

| 项 | 值 |
| :--- | :--- |
| tag tarball | `https://github.com/yaxiaiyuting/bat2sh/archive/refs/tags/v1.10.0a1.tar.gz` |
| sha256（本 session 实测） | `cfd9b83a260f9a03543408366cc2e545f04ee8a90d877b89f89180651ceb5f98` |
| PKGBUILD / .SRCINFO | **未同步**（pre-release 不进 AUR 主分支；`pkgver` 停留 1.9.2） |

## 6. 结论

- 版本号、tag、pre-release 状态三者一致；`Latest` 未被 pre-release 覆盖。
- B1/B2 特性在本机与 CI 双环境均按设计工作；无指标回退、无崩溃、examples 零漂移。
- 发布纪律（tag 打在 bump commit / CI 全绿 / release notes 完整）**全部满足**。

# bat2sh v2.8.0 本机安装验证日志

> 会话：v2.8.0（GUI 视觉优化）。分支 `v2.8.0-gui-visual`；tag `v2.8.0` @ `281c049`。

---

## 一、发布（tag / CI / PKGBUILD / Release）

| 项 | 值 |
| :--- | :--- |
| bump commit | **`281c049`**（`chore: bump version to v2.8.0`） |
| tag | **`v2.8.0`**（annotated） |
| main | FF `94090b6 → 281c049`，已 push |
| **tag 上 CI** | ✅ **success**（run `35425936572`；3.12/3.13/3.14） |
| main CI（bump） | ✅ success（run `35425934534`） |
| main CI（pkg sync） | 见 §一 回填 |
| tarball sha256 | `1d39d21ba2a9c3c56f255ff68152821ba426ffe59f555451c8a7867a661104fd`（二次下载一致） |
| pkg sync | **`fb855f7`**（`makepkg --printsrcinfo` 重生成 .SRCINFO） |
| GitHub Release | **v2.8.0（Latest）** — https://github.com/yaxiaiyuting/bat2sh/releases/tag/v2.8.0 |
| AUR | **不进** |

---

## 二、本机安装验证（系统级 `pacman -U`）

构建：`makepkg -f` → `bat2sh-2.8.0-1-any.pkg.tar.zst`（**223372 B**；
sha256 `f3016e73261820b385aae32aff6fa847ee0a52c89e21037ac2ecd9d6c9018373`）→
`sudo pacman -U --noconfirm` 安装成功（本会话 `db.lck` 已释放，执行**真实系统升级**）。

| # | 项 | 命令 | 结果 |
| ---: | :--- | :--- | :--- |
| 1 | 版本 | `bat2sh --cli --version` | ✅ `bat2sh 2.8.0` |
| 2 | CLI 转换（功能回归） | `bat2sh --cli v280-inst.bat --print` | ✅ 产物含 `#!/usr/bin/env bash` / `ip addr` / `echo "hello"` |
| 3 | GUI 构造 | 安装版模块 + offscreen 构造 `MainWindow` | ✅ 无异常 |
| 4 | GUI 转换 | 打开 `.bat` → 预览 | ✅ 产物含 `hello` |
| 5 | 状态栏计数 | `counts_label.text()` | ✅ `错误 0 · 警告 1 · TODO 0 · 已转换 2/4`（与报告一致） |
| 6 | 布局未重做 | 容器 `layout().spacing()` | ✅ 6；三栏 splitter 结构不变 |

```
installed GUI OK; dark = True ; counts = 错误 0 · 警告 1 · TODO 0 · 已转换 2/4
```

**结论：v2.8.0 安装版系统升级成功；版本 2.8.0、CLI 与 GUI 功能正常、视觉改动生效（spacing=6）。**

> 对比：v2.7.0 会话中 `pacman -U` 曾被**无关挂起事务**锁住而改用等价验证；本会话锁已释放，
> 完成**真实系统安装**并验证，无偏离。

---

> **本文件为 v2.8.0 安装验证日志。**

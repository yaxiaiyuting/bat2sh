# bat2sh v1.10.0 本机安装验证日志（正式版 · 1.x 功能冻结）

> 会话：`v1.10.0 三分支合并 + 收尾`。tag `v1.10.0` 打在 bump commit `683cc53`。
> 环境：CachyOS / Arch，Python 3.14，`bwrap`、`wine`、`makepkg` 均可用。

---

## 1. 版本与时序

| 项 | 值 |
| :--- | :--- |
| 版本号（`pyproject.toml` / `__init__.py`） | **`1.10.0`** |
| merge commits | `29fc7e3`（b1b2）→ `a239ae6`（c4）→ `94b8855`（lex） |
| docs commit | `273dd20`（合并计划 + 收尾判定 + 发布说明 + 总览） |
| bump commit（tag 目标） | **`683cc53`**（`chore: bump version to v1.10.0`） |
| pkg sync commit | `f3284d9`（`chore(pkg): sync PKGBUILD hashes for v1.10.0`） |
| tag | `v1.10.0`（annotated） |
| GitHub Release | **正式版（latest，`isPrerelease:false`）** |
| 发布说明 | `docs/releases/v1.10.0.md` |
| 收尾判定 | **未达成**（`docs/v1.10.0-1x-closure.md`） |
| `release-preflight.sh` | ✅ 通过（空 HOME 类 CI：**1361 passed, 10 skipped**） |

---

## 2. CI（tag 上，head = bump commit）

| run | 事件 | head | 结果 |
| :--- | :--- | :--- | :--- |
| `35228947713` | `push` tag `v1.10.0` | `683cc53` | ✅ **success**（3.12 / 3.13 / 3.14，1m12s） |
| `35228944717` | `push` 分支 `main` | `683cc53` | ✅ success（1m14s） |

---

## 3. 本机实跑（6 项）

| # | 项 | 命令 | 结果 |
| :--- | :--- | :--- | :--- |
| 1 | 源码直跑版本 | `./scripts/bat2sh-dev --version` | `bat2sh 1.10.0` |
| 2 | 模块入口版本 | `PYTHONPATH=python python3 -m bat2sh --version` | `bat2sh 1.10.0` |
| 3 | 免打包安装 | `PREFIX=/tmp/v110/prefix ./install.sh` | 完成（模块/启动器/desktop/图标） |
| 4 | 安装版启动器版本 | `/tmp/v110/prefix/bin/bat2sh --version` | `bat2sh 1.10.0` |
| 5 | 安装版转换 | `/tmp/v110/prefix/bin/bat2sh --cli examples/hello.bat --print` | 产物正确、`bash -n` 通过 |
| 6 | 四个只读子系统 | `validate_*` ×5 = `[]`；`tools/c4` 8/8；`tools/lex` 台账 OK + 4 REPRODUCES | 全绿 |

### 3.1 `makepkg` 打包（PKGBUILD 实跑）

| 项 | 值 |
| :--- | :--- |
| 命令 | `makepkg -f` |
| source sha256 校验 | `bat2sh-1.10.0.tar.gz ... 通过` |
| 产物 | `bat2sh-1.10.0-1-any.pkg.tar.zst`（208 KB） |
| `.PKGINFO` | `pkgver = 1.10.0-1` |
| 内容抽检 | `usr/bin/bat2sh`、`usr/lib/bat2sh/bat2sh/__init__.py`、`usr/share/icons/.../bat2sh.svg` |
| `makepkg --printsrcinfo` | 与仓库 `.SRCINFO` 一致（`pkgver=1.10.0` + sha256） |

---

## 4. 指标快照（合并后 `main` = tag 树，独立仪器 `/tmp/v110/measure.py`）

| 指标 | 值 |
| :--- | ---: |
| 语料 | 151 |
| 语法通过 | **147/151** |
| rc==0（原始） | **102** |
| 功能完好（严格） | **16** |
| degraded | **86**（degraded 内 TODO **894**） |
| rc≠0 | 45 |
| 崩溃 / 超时 | 0 / 0 |
| 转换产物逐字节 diff（vs `session-lex`） | **0 文件** |
| examples tracked 漂移 | **0**（三支均未触碰 `examples/`） |
| wine 黄金对照 | **6/6 MATCH** |
| pytest（本机完整） | **1371 passed** |

---

## 5. 发布 tarball 与 AUR

| 项 | 值 |
| :--- | :--- |
| tarball | `https://github.com/yaxiaiyuting/bat2sh/archive/refs/tags/v1.10.0.tar.gz` |
| sha256 | `49d8cfa10ae2feb6c1dd432b9108867f5bb4f24bf840d54cf0b2e50a618e4d0b` |
| `PKGBUILD` / `.SRCINFO` | **仓库侧已同步**（`pkgver=1.10.0`，sha256 对齐，`.SRCINFO` 由 `makepkg --printsrcinfo` 重生成） |
| **AUR 远端推送** | **未执行**：AUR 上不存在 `bat2sh` 包（RPC 查询 resultcount=0）、本机无 AUR remote、`~/.ssh` 无私钥（`Permission denied (publickey)`）。历史「sync PKGBUILD」commit 亦仅改仓库内文件。见 `docs/v1.10.0-report.md` §AUR。 |

---

## 6. 纪律对照

| 纪律 | 落实 |
| :--- | :--- |
| 仪器先验证 | 语料用 `/tmp/v110/measure.py`（复制自 `/tmp/lex`，与 `/tmp/c4`、`/tmp/lex` 逐项一致）；wine 6/6；`makepkg` source sha256 校验通过 |
| 合并后必须全量验证 | 三支各自 pytest 1344/1360/1371 → 合并后 1371；语料逐字节 0 diff；wine 6/6 |
| 锁定 HEAD | 起点 `37e3564`；每次写前记录 `git rev-parse HEAD`（`94b8855 → 273dd20 → 683cc53 → f3284d9`），无并发变化 |
| 主动披露偏离 | ① 三分支为**累积/堆叠**而非独立分叉（冲突构造上为 0）；② **AUR 远端推送不可执行**（无包/无凭据）；③ 发布说明原拟「1.x 收尾完成」经核**更正为「功能冻结、收尾未达成」** |
| 不 force-push | 未使用 |
| 保守 TODO 优于激进转换 | 本版零转换改动；不硬凑指标 |

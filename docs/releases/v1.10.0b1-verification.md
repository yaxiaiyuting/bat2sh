# bat2sh v1.10.0b1 本机安装验证日志（pre-release）

> 会话：`session-c4`（C4 goto 控制流形态台账）。tag `v1.10.0b1` 打在 `8fdae5f`。

---

## 1. 版本与时序

| 项 | 值 |
| :--- | :--- |
| 版本号（`pyproject.toml` / `__init__.py`） | `1.10.0b1` |
| bump commit（tag 目标） | `8fdae5f`（`chore: bump version to v1.10.0b1`） |
| tag | `v1.10.0b1`（annotated） |
| 分支 | `session-c4`（已 push） |
| GitHub Release | **pre-release**（`isPrerelease:true`、`isDraft:false`）；`Latest` 仍为 v1.9.2 |
| 发布说明 | `docs/releases/v1.10.0b1.md` |

## 2. 本机实跑（5 项）

| # | 项 | 命令 | 结果 |
| :--- | :--- | :--- | :--- |
| 1 | 源码直跑版本 | `./scripts/bat2sh-dev --version` | `bat2sh 1.10.0b1` |
| 2 | 模块入口版本 | `PYTHONPATH=python python -m bat2sh --version` | `bat2sh 1.10.0b1` |
| 3 | 免打包安装 | `PREFIX=/tmp/c4/prefix ./install.sh` | 完成（模块/启动器/desktop/图标） |
| 4 | 安装版启动器版本 | `/tmp/c4/prefix/bin/bat2sh --version` | `bat2sh 1.10.0b1` |
| 5 | 安装版转换（C4 相关：goto 诚实 TODO 不变） | `/tmp/c4/prefix/bin/bat2sh --cli goto_demo.bat --print` | 见下，`bash -n` 通过 |

输入 `goto_demo.bat`：

```bat
@echo off
set n=0
:loop
set /a n=%n%+1
if %n% gtr 3 goto done
echo n=%n%
goto loop
:done
echo finished
```

产物（节选，`bash -n` 通过）：

```bash
set -euo pipefail

n="0"
# :loop（goto 目标，不函数化，主流程继续）
n=$(( ${n}+1 ))
if [ "${n}" -gt 3 ]; then
    # TODO: 手动检查: goto done
:
fi
echo "n=${n}"
# TODO: 手动检查: goto loop
# 注意：以下代码原被 goto :loop 跳过，在 bash 中会执行，请核对
# :done（goto 目标，不函数化，主流程继续）
echo "finished"
```

- goto **仍为诚实 TODO**（本版**零转换改动**，语义与 v1.10.0a1 完全一致）；
- 前向 skip 保留了既有「以下代码在 bash 中会执行，请核对」提示；
- 报告：转换 9 行 / 错误 0 / TODO 2（`goto done` / `goto loop`）。

## 3. 回归与门槛

| 项 | 值 |
| :--- | :--- |
| pytest（本机） | **1360 passed**（v1.10.0a1 基线 1344；+16） |
| `release-preflight.sh`（类 CI 空 HOME） | 通过（1350 passed / 10 skipped） |
| wine 黄金对照 harness | **6/6 MATCH**（g01–g06；g05 `%VAR%` 冻结未回退） |
| 151 语料 | 语法 147/151、rc==0 102、功能完好 **16**、degraded 86、TODO 894、崩溃/超时 0 |
| 151 语料产物 diff vs session-b1b2 | **0 文件**（零转换改动） |
| examples tracked 对 | 3/3 零字节漂移 |
| `validate_control_flow_patterns()` | `[]` |
| `tools/c4/control_flow_report.py` | 台账 8/8 OK（常量 == 实测） |

## 4. CI 时序（tag 上）

| 时间（UTC） | 事件 |
| :--- | :--- |
| 00:34:51 | 分支 `session-c4` push → 触发 `push`（run `35167132968`，**CI 修正生效**） |
| 00:34:54 | tag `v1.10.0b1` push → 触发 `push`（run `35167135787`，**CI 修正生效**） |
| 00:36:03 / 00:36:03 / 00:36:03 | **test (3.12) / (3.13) / (3.14) 全部 success** |
| 00:36:38 | GitHub Release `v1.10.0b1` 标记 pre-release |

> **CI 修正生效证据**：本 session 之前，`push` 仅触发 `main`（分支/tag push 不触发，
> v1.10.0a1 需靠草稿 PR 取得覆盖）。本版 `push: branches: [main, session-*] + tags: [v*]`
> 后，**分支与 tag push 均直接触发 CI 并全绿**（上表两条 run 均为 `push` 事件）。

## 5. 包元数据（如需后续 AUR 同步）

| 项 | 值 |
| :--- | :--- |
| tag tarball | `https://github.com/yaxiaiyuting/bat2sh/archive/refs/tags/v1.10.0b1.tar.gz` |
| sha256（本 session 实测） | `7e68243b2337110acf745720796f54cb9a94c2b91d4166caac393883bd07db33` |
| PKGBUILD / .SRCINFO | **未同步**（pre-release 不进 AUR 主分支；`pkgver` 停留 1.9.2） |

## 6. 结论

- 版本号、tag、pre-release 状态三者一致；`Latest` 未被 pre-release 覆盖。
- C4 台账子系统在本机与 CI 双环境均按设计工作；**零转换改动、无指标回退、无崩溃、examples 零漂移**。
- CI 触发修正**经实跑验证生效**（分支/tag push 直接触发）。
- 发布纪律（tag 打在 bump commit / CI 全绿 / release notes 完整）**全部满足**。

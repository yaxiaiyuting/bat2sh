# bat2sh v2.8.1 安装验证日志

> 会话：PS 评估收尾 + try/catch A 类缺陷修复。分支 `main`；tag `v2.8.1` @ `e628e39`。

---

## 一、发布（tag / CI / PKGBUILD / Release）

| 项 | 值 |
| :--- | :--- |
| 评估文档 commit | **`a919050`**（PS 解冻评估 4 份，判定：继续冻结） |
| fix commit | **`c0d7fb3`**（PS 报告诚实性修复） |
| bump commit | **`e628e39`**（`chore: bump version to v2.8.1`） |
| tag | **`v2.8.1`**（annotated） |
| main | `46d5d21 → e628e39`，已 push |
| **tag 上 CI** | ✅ **success**（run `35429963246`；3.12/3.13/3.14） |
| main CI（bump） | ✅ success（run `35429961583`） |
| main CI（pkg sync） | ✅ success（run `35430069124`） |
| tarball sha256 | `cf6a3e29ce9eb64009645b3e2ffa48acb00a1c02bc5b5c1353bb08d971861c64`（二次下载一致） |
| pkg sync | **`0d06a4c`**（`makepkg --printsrcinfo` 重生成 .SRCINFO） |
| GitHub Release | **v2.8.1（Latest）** — https://github.com/yaxiaiyuting/bat2sh/releases/tag/v2.8.1 |
| AUR | **不进** |

---

## 二、本机安装验证（系统级 `pacman -U`）

构建：`makepkg -f` → `bat2sh-2.8.1-1-any.pkg.tar.zst`（**223248 B**；
sha256 `c7a98683ed1e5038026ca45578fa55433ac9dfba0452bb403ddd1ba8eb252939`）→
`sudo pacman -U --noconfirm` 安装成功。

| # | 项 | 命令 | 结果 |
| ---: | :--- | :--- | :--- |
| 1 | 版本 | `bat2sh --cli --version` | ✅ `bat2sh 2.8.1` |
| 2 | **修复：CI 门** | `bat2sh --cli cd-up.ps1 --print --fail-on-todo` | ✅ **exit 3**（修复前 0） |
| 3 | **修复：报告计数** | `... --print --report-json` | ✅ `todo_count 1 / warning_count 4 / error_count 0` |
| 4 | 产物标记 | `... --print \| grep '# TODO'`（排除脚本头） | ✅ 2 个标记（try + catch），均有对应 todo |
| 5 | bat 回归：TODO 门 | `.bat`（含 TODO）`--fail-on-todo` | ✅ exit 3（未回退） |
| 6 | bat 回归：干净门 | `.bat`（无 TODO）`--fail-on-todo` | ✅ exit 0 |

```
installed: bat2sh 2.8.1
PS cd-up.ps1 --fail-on-todo → 3 ; todo_count 1
bat TODO gate → 3 ; bat clean gate → 0
```

**结论：v2.8.1 安装版验证通过；PS `--fail-on-todo` 静默失效已修复，bat 侧无回归。**

---

## 三、验收

| 项 | 结果 |
| :--- | :--- |
| pytest | ✅ **1561 passed**（+7 回归） |
| preflight | ✅ 1544 passed / 17 skipped（类 CI 空 HOME） |
| PS 语法口径未回退 | ✅ 56/60 = 93.3% |
| 全量 664 defect（markers>0 且 todos==0） | ✅ 8 → **0** |
| 151 bat 语料 | ✅ 151/149/101/19/82/851/崩溃 0（未触 bat） |
| 053/A1 | ✅ 未触（PS 路径独立） |
| 无新依赖 | ✅ |

---

> **本文件为 v2.8.1 安装验证日志。**

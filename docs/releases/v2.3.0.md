## bat2sh v2.3.0

> **性质**：`2.3.0` **minor**。本版采用 **「实测优先」协议（纪律 18）**，并**触发 2.x 收尾评估**。
> **结论**：v2.3.0 提议项（goto CFG 高级 / B5 / 注册表写逐键）**全部实测翻转 0**；
> `goto CFG 高级` = 「**语义完整但翻转 0**」→ 归**终态（仅 T1 真实需求触发重评）**。
> **✅ 2.x 收尾成立**：三条件 (a)(b)(c) 全 ✓ → 冻结**维护模式**（见 `docs/v2.3.0-2x-closure.md`）。
> **本版零代码改动**（纯文档 + tag）：151 语料产物逐字节不变。
> **AUR**：本版**不进 AUR**（同 v2.x 前序版本）。

---

## 1. 逐项实测结果

| 项 | roadmap 估算 | **实测翻转** | 关键实测 | 裁定 |
| :--- | :--- | :---: | :--- | :--- |
| goto 回跳 | 0–2 | **0**（候选 1） | `446/28`；候选须在标签处发射循环 → 触 `_label_line`(053) | 终态（T1 重评） |
| goto 块内 | 0–2 | **0** | `457/8`；0 rc==0 单桶；053 同域 | 终态（T1 重评） |
| **goto CFG 高级**（多入口/多出口） | ？ | **0** | 15 多入口文件**全为多设施/语法失败**；唯一 goto 单桶文件**无多入口** | **语义完整但翻转 0 → 终态（T1 重评）** |
| B5 括号跨行累积 | ？ | **0** | 定向 `_logical_lines` 探针：churn=1 文件、**语法仍失败**（第二缺陷独立） | 终态（解析层重写） |
| 注册表写逐键 | 「11 可能翻转」 | **0** | 29 条全写类/导出；Linux 无注册表；已结构化 `# TODO[REG]` | 终态（维持） |

---

## 2. 2.x 收尾声明

> **bat2sh 2.x 线收尾成立**，冻结为**维护模式**。判据：三条件全 ✓ + 逐项实测翻转 0（证据链完整）。

| 条件 | 判定 | 依据 |
| :--- | :---: | :--- |
| (a) 所有剩余失败有明确归属 | **✓** | 语法失败 2/2；degraded 85 全按桶归属；无「无归属」 |
| (b) 无「可修但未修」 | **✓** | 2.x 可做且翻转>0 = 5（全做）；三轮实测可做翻转全 0 |
| (c) 指标稳定且无提升空间 | **✓** | `degraded = 85` = 实测下界 |

**2.x 交付回顾**：v2.0.0 解析层硬化（+2 语法通过）｜v2.1.0 CFG 只读数据模型｜v2.2.0 `sc` 结构化诚实 TODO｜
v2.3.0 逐项实测 + 收尾。

---

## 3. goto CFG 高级的归属（关键）

> **终态（仅 T1 真实需求触发重评）。**

- 实测翻转 **0**：15 个多入口标签文件全部是多设施（`系统优化` 443 TODO）或语法失败（`史上最牛X`）；
  唯一 goto 单桶可翻转文件（`显示所有分区的剩余空间`）**无多入口**，纯被 053（`_label_line`）阻塞。
- 成本 **15–25 人日** 换 **0** 翻转 → 违反纪律 1/17。
- 起步设施（T1 时）：`core/cfg.py`（v2.1.0）+ `tools/cfg`。

---

## 4. 六次估算错误统计（延续 v2.2.0）

| # | 版本 | 估算（错） | 实测 | 来源 |
| ---: | :--- | :--- | :--- | :--- |
| 1 | v1.8.1 | 静默错误率「8–9%」 | 实测修订 | `v1.8.2-artifact-classification.md:438` |
| 2 | v1.9.1 | 「1.x 可解 = 46 文件」 | **25** | `v1.11.0-1x-closure-final.md:148` |
| 3 | v1.11.0 | 标准 1「≥150/151」 | **147/151** | `v1.11.0-standards-revision.md` |
| 4 | v1.11.0 | 标准 3「degraded ≤41」 | **83** | `v1.11.0-standard-3-revision.md` |
| 5 | v2.0.0 | 「语法失败 4→1」 | **2** | `v2.0.0-report.md` |
| 6 | v2.1.0 | 「goto 翻转 ≈3–4」 | **0** | `v2.1.0-report.md` |

> 2.x 三轮（v2.1–v2.3）实测翻转均为 0，再次印证 **「不采信未实测的翻转数字」**。

---

## 5. 151 语料指标（分母 = 语法通过数）

| 指标 | v2.2.0 | **v2.3.0** | 变化 |
| :--- | ---: | ---: | :--- |
| 语料 | 151 | 151 | — |
| **语法通过** | 149 | **149** | 0 |
| rc==0（原始） | 104 | **104** | 0 |
| **功能完好（strict）** | 19 | **19** | 0 |
| degraded | 85 | **85** | 0 |
| degraded TODO | 897 | **897** | 0 |
| 崩溃 / 超时 | 0 / 0 | **0 / 0** | 0 |
| pytest | 1448 | **1448** | 0（本版无测试改动） |
| **wine 黄金对照** | 6/6 | **6/6** | 0 |
| examples tracked 对 | 零漂移 | **零漂移** | 0 |
| 转换产物 diff | — | **0 文件** | ✅ 零代码改动 |

**实测翻转数 = 0**（如实记录，不硬凑，纪律 13）。

---

## 6. 破坏性变更

**无。** 本版**零代码改动**（仅 docs + 版本号），无产物/CLI/API 变化，未触 053/A1。

---

## 7. 已知限制

- goto（全部）/ B5 / 注册表写 / 名称映射 / B2 输出适配 / L4 / thirdparty —— **均归终态**，且均有实测依据。
- `史上最牛X…bat` 仍语法失败（多缺陷解析层）；`运行剪贴板中的bat.bat` 终态（B 类源畸形）。
- **PS 维持实验性支持**（60 PS 语料语法 56/60 = 93.3%；运行时语义 73.7% 实验口径，见 §8）。
- 2.x 需求驱动启动条件（T1/T2/T3）不变。

---

## 8. PS 解冻评估（下一个大方向）

| 维度 | 现状 |
| :--- | :--- |
| 定位 | **实验性支持**（产物「高级草稿」） |
| 语法口径（60 PS 语料） | 原始 **56/60 = 93.3%**；降级 60/60 |
| 运行时语义（实验） | 73.7%（v1.8.0 口径，bwrap + pwsh） |
| D 档缺失 | 对象模型 / `.NET` / 注册表 / 远程会话 |
| 触发条件 | **T3**（PS 侧明确刚需）—— **当前未触发** |
| 建议 | 启动前先做 **PS 侧只读评估 session**（逐条归属 + 需求确认）；**本版不启动** |

---

## 9. 后续

- **2.x**：**维护模式**（仅缺陷修复 + 文档 + 语料基线维护）。
- **3.x**：**唯一大方向 = PS 解冻**（需求驱动，未触发）；解析层重写（仅 T1）。
- 详见 `docs/v2.3.0-2x-closure.md` 与 `docs/v2.3.0-report.md`。

**Full Changelog**: https://github.com/yaxiaiyuting/bat2sh/compare/v2.2.0...v2.3.0

# bat2sh v1.9.2 本机安装验证日志

> 目的：在真实本机（CachyOS/Arch，Python 3.14）验证 v1.9.2 的**安装可用性**与
> **关键语义修复的实跑行为**。发布说明见 `docs/releases/v1.9.2.md`。

---

## 1. 版本

| 项 | 值 |
| :--- | :--- |
| 仓库 HEAD（bump） | `84ea911`（tag `v1.9.2`） |
| PKGBUILD 同步 | `bded029`（sha256 已同步 tarball，`.SRCINFO` 已重生成） |
| `pyproject.toml` / `__init__.py` | `1.9.2` |
| 安装方式 | `./install.sh`（默认 `~/.local`） |
| `bat2sh --version` | **`bat2sh 1.9.2`** ✅ |

---

## 2. 关键实跑（5 项）

> 全部在 `~/.local/bin/bat2sh`（安装版）上执行。

| # | 项 | 输入 | 结果 | 判定 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | **A1 `%VAR%` 块内冻结** | `set v=1` + `for %%i in (a b) do (set v=2 & echo i=%v%)` | 运行输出 **`i=1` / `i=1`**（与 wine cmd 一致） | ✅ |
| 2 | **`%ERRORLEVEL%` 默认映射** | `echo rc=%ERRORLEVEL%` | 0 条 `# TODO`；产物含 `__bat2sh_rc=$?` + `echo "rc=${__bat2sh_rc}"` | ✅ |
| 3 | **`%date:~..%` 诚实 TODO** | `set bak_date=%date:~0,4%%date:~5,2%` | 产物为 `# TODO: 手动检查: …`（**无字面量泄漏**） | ✅ |
| 4 | `--fail-on-todo` 退出码 | 上述 `dt.bat` + `--print --fail-on-todo` | **exit = 3** | ✅ |
| 5 | 版本 | `bat2sh --version` | `bat2sh 1.9.2` | ✅ |

---

## 3. 黄金对照 harness（wine）

```
python3 tools/oracle/golden_harness.py
g01..g06: 6/6 MATCH（g05 = A1 用例，v1.9.2 前为 DIFF）
```

无 wine 环境（CI / 空 HOME）自动 **skip 而非 fail**（`wine_available()` 要求默认 prefix 存在）。

---

## 4. 门槛与回归

| 项 | 值 |
| :--- | :--- |
| `scripts/release-preflight.sh`（空 HOME 类 CI） | **通过**（1252 passed, 10 skipped） |
| 本机 `pytest -q`（完整，含 wine） | **1262 passed** |
| 语法通过 | 147/151 |
| rc==0（原始） / 功能完好 / degraded | 102 / 16 / 86 |
| degraded TODO 总数 | 913 → 895（−18） |
| L1（errorlevel + 逸出） | 38 → 0（errorlevel 25→0；逸出 13→4 重分类 L3b） |
| 崩溃 / 超时 | 0 / 0 |
| examples | 5/5 零字节漂移（`stress_test.sh` 因 errorlevel 映射有意刷新） |

---

## 5. CI（tag 上确认）

| 项 | 值 |
| :--- | :--- |
| 触发 | `push: branches: [main]`（workflow 见 `.github/workflows/test.yml`） |
| run（bump commit `84ea911`） | `35124273107` |
| Python 3.12 / 3.13 / 3.14 | **全 success** |
| 备注 | 仅有 `actions/*` 的 Node.js 20 弃用提示（非失败） |

GitHub Release：https://github.com/yaxiaiyuting/bat2sh/releases/tag/v1.9.2

---

## 6. 发布 tarball

| 项 | 值 |
| :--- | :--- |
| 来源 | `https://github.com/yaxiaiyuting/bat2sh/archive/refs/tags/v1.9.2.tar.gz` |
| sha256 | `b362d2d5e2ea39fd303f1cf23791b368f5ebe85086e222f4a9ba04f0626db99a` |
| PKGBUILD / `.SRCINFO` | 已同步（`makepkg --printsrcinfo` 重生成，diff 一致） |

---

## 7. 纪律对照

| 纪律 | 落实 |
| :--- | :--- |
| 仪器先验证 | 语料指标由独立测量脚本（6/6 已知样本盲测）复跑；黄金对照 6/6 |
| 主动披露偏离 | 无（发布前置、CI、Release 均按流程完成） |
| 锁定 HEAD | 会话内每次写前校验；记录 HEAD 序列 `444cc6c → … → bded029` |
| rc==0 严格口径 | 语料指标以「功能完好 / degraded」双列记录 |

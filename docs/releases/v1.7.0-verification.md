# bat2sh v1.7.0 本机安装验证日志

环境：CachyOS / Arch Linux，Python 3.14.7，`./install.sh`（用户级安装到 `~/.local`）。

## 1. 版本确认

```console
$ ~/.local/bin/bat2sh --version
bat2sh 1.7.0
$ which bat2sh
/home/duanjb666/.local/bin/bat2sh
```

## 2. 一次并行修复实跑（安装版）

场景：2 条可修复 TODO 的 `.bat`，`--max-concurrency 3`，本地 OpenAI 兼容 SSE mock
（不访问外网）；用 pty 驱动真实交互（多选 → 隐私确认 → 应用确认）。

命令：

```bash
bat2sh --cli /tmp/bat2sh-e2e/demo.bat --fix-todos \
  --api-base http://127.0.0.1:8765/v1 --api-model mock-model --max-concurrency 3
```

实测输出（已去除 ANSI 控制序列；面板为原地刷新，此处为最终帧）：

```text
1. 第 6 行: # TODO: 手动检查: for /f "usebackq" %%i in (`dir /b`) do echo %%i
2. 第 7 行: # TODO: 手动检查: for /f "tokens=*" %%j in (`dir /s /b`) do echo %%j
bat2sh: 选择要修复的条目（如 1,3-5；回车=全部；q=取消）[all]
bat2sh: 即将把 2 条 TODO 发送到外部服务：http://127.0.0.1:8765/v1
bat2sh: 内容将离开本机，可能被服务提供方记录（prompt 不含 API key）。
── 将发送的条目 ────────────
#1 第 6 行: # TODO: 手动检查: for /f "usebackq" %%i in (`dir /b`) do echo %%i
#2 第 7 行: # TODO: 手动检查: for /f "tokens=*" %%j in (`dir /s /b`) do echo %%j
───────────────────────────
bat2sh: 发送 2 条到 http://127.0.0.1:8765/v1？[y/N] y
bat2sh: 并发 3（--max-concurrency 可调）

#1 第6行 running

#2 第7行 pending

#1 第6行 running

#2 第7行 running

#1 第6行 running | echo "fixed-a"

#2 第7行 running

#1 第6行 running | echo "fixed-a"

#2 第7行 running | echo "fixed-b"

#1 第6行 done

#2 第7行 running | echo "fixed-b"

#1 第6行 done

#2 第7行 running | echo "fixed-b" echo "fixed-b2"

#1 第6行 done

#2 第7行 done
--- 当前
+++ 建议
@@ -4,4 +4,5 @@
 set -euo pipefail

-# TODO: 手动检查: for /f "usebackq" %%i in (`dir /b`) do echo %%i
-# TODO: 手动检查: for /f "tokens=*" %%j in (`dir /s /b`) do echo %%j
+echo "fixed-a"
+echo "fixed-b"
+echo "fixed-b2"
bat2sh: 应用全部 2 处修改？[y/N] [已写出] /tmp/bat2sh-e2e/demo.sh（API 修复 2 处，请复核后再使用）
bat2sh: TODO 处理完成：修复 2 / 跳过 0 / 失败 0（共 2 条选中）
bat2sh: 注意：API 建议仍需人工复核，不保证语义正确。

============================================================
exit code: 0
output exists: True
--- demo.sh ---
#!/usr/bin/env bash
# 由 bat2sh 自动转换生成，源文件: demo.bat
# 带有 # TODO 标记的行无法自动转换，请人工检查
set -euo pipefail

echo "fixed-a"
echo "fixed-b"
echo "fixed-b2"
```

## 3. 发布门槛数据

| 门槛 | 结果 |
| --- | --- |
| pytest | **1087 passed**（v1.6.0 基线 1048，新增 39） |
| examples 全量刷新 | 与已提交 `examples/*.sh` **零字节漂移**，全部通过 `bash -n` |
| 真实语料交叉验证 | 114 个独立脚本；语法通过率 **100%**；转换崩溃 **0**；静默错误率沿用归档口径 **18.5%** |
| CI | GitHub Actions Python **3.12 / 3.13 / 3.14 全绿** |
| 并行修复实跑 | 退出码 0；产物通过 `bash -n`；高行号条目 2 行替换后**从后往前合并**正确 |

退出码说明：`0` 全部修复；`3` 仍有跳过/失败或拒绝应用；`6` 配置错误。

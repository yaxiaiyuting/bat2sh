# P0 修复结果 —— F1/F2/F3（+F5）与 oracle 重验

> 时间：2026-09-27 · 轨道：**产品**（改 bat2sh 代码）· 起点 HEAD `9c096a4` → 终点 `f7e6c86`
> 诊断：`p0-diagnosis.md`（含 §12 硬闸门裁定 G1–G4）
> 本文：**Phase 2/3 结果** + oracle 重验（真机 W vs 产物 L）
> 交付：4 个 commit · 12 条新测试 · 4 条断言按纪律 9 重写 · oracle 5 样本重跑

---

## 1. 一句话结论

> **oracle 的 5 个样本，修复前 `不可归因 4 / 可归因 1 / 一致 0`，修复后 `不可归因 0 / 可归因 3 / 一致 2`。**
> 三个缺陷（F1/F2/F3）在**真机差分**上全部消失，**零新增不一致**；
> 151 语料指标 `151/149/101/19/82/851/crash 0` **逐字段不变**，examples **零字节漂移**，
> A1 覆盖 7 文件的快照**赋值行 7/7 完全一致**。
> 另按 G2 裁定一并修掉诊断期新发现的 **F5**（`\` 后接变量展开，14 文件 / 48 处）。

---

## 2. 四个 commit

| # | commit | 内容 | 代码锚点 |
| :-: | :--- | :--- | :--- |
| 1 | `ea9782c` | `fix(F1): type 命令的 \ 未转换` | `batch.py cmd_type` |
| 2 | `4f0c8ee` | `fix(F2): for /d 集合末元素被当成通配符` | `batch.py _convert_for` `/d` 分支 |
| 3 | `743abc1` | `fix(F5): \ 后接变量展开时未转换为路径分隔符` | `utils.py convert_backslashes` + `_is_substitution` |
| 4 | `f7e6c86` | `fix(F3): for /r + %%~ni 产出错误文件名` | `batch.py _emit_for_r` |

**顺序偏离（纪律 4，已在 `p0-diagnosis.md` §12.2 预告）**：任务书列 F1→F2→**F3**→F5，
实做改为 F1→F2→**F5**→F3 —— 因为 **F5 是 F3 的使能修复**（F3 的 `\` 分隔符与 `%%~ni`
必须同时修好才不产出错文件名），且 F5 的通用写法**完全覆盖** F3-a 的窄修。
若按原序，commit 4 必须回退 commit 3 的代码（两种写法在 `\\%VAR%` 上行为不一致）。

### 2.1 端点自检（每个 commit 前）

| # | 自检 | F1 | F2 | F5 | F3 |
| :-: | :--- | :--- | :--- | :--- | :--- |
| 1 | 触及 053/A1 了吗？防护到位吗？ | 否 | ⚠️ 053 函数级 → 四道检测 | ⚠️ A1 邻域 → 四道检测 + 观察名单 | ⚠️ 053 函数级 → 四道检测 |
| 2 | 有更保守的方案吗？ | 已是最小（与 `BATCH_SIMPLE_MAP` 分支逐字同构） | 已是最小（逐元素替换整串） | 否决了 `_expand_vars` 预转换（漂移 17 vs 13） | 否决了 `find "$PWD"/root`（绝对 root 会坏） |
| 3 | examples 零漂移吗？ | ✅ 0 | ✅ 0 | ✅ 0 | ✅ 0 |
| 4 | 现有测试全绿吗？ | ✅ | ✅ | ✅ | ✅（4 条断言按纪律 9 重写） |
| 5 | commit message 准确吗？ | ✅ 含根因/证据/检测 | ✅ 同 | ✅ 同 + 边界理由 | ✅ 同 + 已知限制 |

---

## 3. oracle 重验（Phase 3）

### 3.1 方法

按 `oracle-samples.md` §3 重跑 **V1–V5 两侧**（W 真机 + L 产物沙箱）：

```bash
# L（bwrap，产物侧）
python3 tools/collect_linux.py --script <SRC> --guest-name <v.bat> \
        --bat2sh /tmp/p0/bin/bat2sh --output results/oracle/L-v2110/<v>.json
# W（真机 cmd.exe，QEMU/QGA）
python3 tools/collect.py --sample <SRC> --output results/oracle/W-v2110/<v>.json \
        --guest-name <v.bat> --scope 'C:\poc\samples' --network isolated
# 三分类
python3 tools/compare.py --w …/W-v2110/<v>.json --l …/L-v2110/<v>.json \
        --report results/oracle/report-v2110/<v>.json
```

两侧 `harness.git_head` **全部对齐 `f7e6c864`**（5/5），W 侧 5 次采集全部 rc=0、用时 44–53 s。

### 3.2 ⚠️ 仪器变更（**必须披露**）

| # | 变更 | 原因 | 影响 |
| :-: | :--- | :--- | :--- |
| **I1** | `collect_linux.py` 新增 `--bat2sh`，并把 `harness.convert_tool`（**解析路径 + `--version`**）写进指纹 | **首次重跑得到的是"未修复"的结果**：`convert()` 硬编码调用 PATH 上的 `bat2sh`（= 系统已装的 v2.10.0），**根本没用到工作树**。若不发现，本 session 会得出"修复无效"的**错误结论** | 指纹从此记录"用哪个转换器"，堵住复现缺口。默认值不变（PATH 上的 `bat2sh`），旧指纹不受影响 |
| **I2** | `compare.py` 新增规则 **N15** 与 D 条目 **D15**（盘符不映射） | 上一轮 `oracle-result.md` §3.4/§5 已把 F4 **裁定为已知行为**并写明"补入 D-list 作 D15"，但**当时未接进工具** ⇒ V4 恒报"不可归因"，把**已裁定的产品行为**与真缺陷混在一起 | **刻意放在归因档**（不是严格档）：F4 是**语义**分歧（写到工作根之外 vs 之内），绝不能被判成"一致"。窄判据：只放行 `<WORK>` 下首段恰为单字母 `X:` 的路径 |
| **I3** | `fs_names` / `fs_content` 的归因改为**最小规则集**（逐条单选，命中即止） | 原实现只试 N6（名称）/ 只试 N10→N11（内容），新增规则无处生效；且朴素累加会让报告堆出 `N15,N2,N3` 之类无关组合，**失去判别力** | 报告里每条生效规则都带"**掩盖了什么**" |

> **I1 是本 session 最有价值的一次"仪器先验证"**：`collect_linux.py` 的输出**看上去完全正常**
> （rc=0、指纹文件写好了、字段齐全），只有把结果与预期对照才发现它用的是**已安装的旧版**。
> 这正是 `oracle-verdict.md` §4.3 那条"仪器先验证"要防的事。

### 3.3 结果（修复前 → 修复后）

| 样本 | 覆盖 | 修复前 | 修复后 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| **V1** `快速创建文件夹.bat` | 建目录 | ❌ 不可归因（**F2**） | ✅ **一致** | 两侧都建出 `樱野`/`王子变青蛙`/`其他` **3 个**目录 |
| **V2** `生成指定内容的文本文件.bat` | 中文文本落盘 | 🟡 可归因（D3） | 🟡 **可归因（D3）** | **保持**。`W.Hash == L.hash_cp936_crlf` 精确相等 |
| **V3** `poc-02-fileops.bat` | 写/读/复制/删除 | ❌ 不可归因（**F1**+D5） | 🟡 **可归因（D2）** | rc 1→0、stdout 恢复 `beta`、`b.txt` 被删 |
| **V4** `copy.bat` | 建/复制/读 + 根外探针 | ❌ 不可归因（**F1**+F4） | 🟡 **可归因（D15+D2）** | F1 消失；剩余 = **上一轮已裁定的 F4** |
| **V5** `以文件夹名为名建立文本文件.cmd` | 递归遍历 | ❌ 不可归因（**F3**） | ✅ **一致** | 两侧都建出 `samples.txt` |

```
              修复前   修复后
一致            0   →    2     (V1 V5)
可归因          1   →    3     (V2 V3 V4)
不可归因        4   →    0     ← 清零
```

### 3.4 通道级明细（修复后）

| 样本 | rc | stdout | stderr | fs 名称 | fs 内容 | 生效规则 → D |
| :--- | :-: | :-: | :-: | :-: | :-: | :--- |
| V1 | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| V2 | ✅ | ✅ | ✅ | ✅ | 🟡 N11 | N11 → D3 |
| V3 | ✅ | ✅ | ✅ | ✅ | 🟡 N10 | N10 → D2 |
| V4 | ✅ | ✅ | ✅ | 🟡 **N15** | 🟡 N10+N15 | N15 → **D15**；N10 → D2 |
| V5 | ✅ | ✅ | ✅ | ✅ | ✅ | — |

> V4 的 `fs_names` 在 N15 下两侧**逐项相等**：
> `created=["<WORK>/work/a.txt","<WORK>/work/c.txt"]`、`created_dirs=["<WORK>/work"]`。
> 原始差异（L 多出 `<WORK>/C:`、`<WORK>/C:/iso-probe/…`）**未被抹平** ——
> 报告仍完整打印 N15 的"掩盖了什么"。

### 3.5 "零新增不一致"的论证

| 判据 | 结果 |
| :--- | :--- |
| 修复前 `不可归因` 的 4 个样本 | V1/V3/V4/V5 —— 全部改善或持平（0 个变差） |
| 修复前 `可归因` 的 V2 | 保持 `可归因(D3)`，**规则与 D 条目完全未变** |
| 新增 `不可归因` | **0** |
| 两侧 git_head | W/L 5 对**全部对齐** `f7e6c864` |
| 仪器确定性 | `compare.py` 连跑两次**逐字节一致**（v1/v3/v4 实测） |
| 仪器回归自检 | 用**新工具**跑**上一轮指纹**：V1/V3/V5 仍判"不可归因"、V2 仍"可归因(D3)" ⇒ **是修复而不是工具放水让分类变了** |

---

## 4. 全量回归（Phase 3）

| 项 | 基线（`9c096a4`） | 修复后（`f7e6c86`） | 判据 |
| :--- | :--- | :--- | :--- |
| pytest | 1613 passed | **1625 passed**（+12 新测试，0 failed） | ≥ 基线，只增不减 ✅ |
| 053+A1 专项（12 文件） | — | **117 passed / 0 failed** ✅ | 0 failed |
| A1 变量展开旁路 | — | 18 passed ✅ | 0 failed |
| 151 语料 summary | `151/149/101/19/82/851/crash 0` | **逐字段完全相同** ✅ | 六项全等 |
| 逐文件判据（rc/syntax_ok/todo_count/markers/todos） | — | **0/151 变化** ✅ | 零变化 |
| run 产物字节 | — | 14/149 变化，**逐条可归因** ✅ | 见 §4.1 |
| 全量 155 文件 `todo_count` | — | **0 变化** ✅ | 零变化 |
| examples | — | **0 字节漂移** ✅ | 红线 |
| wine oracle | — | **6/6 MATCH**（`test_oracle_wine.py` 34 passed 含 examples） ✅ | 红线 |
| §3.4 A1 覆盖 7 文件 —— 快照**赋值**行 | — | **7/7 完全一致** ✅ | A1 语义零变化 |
| `bash -n` 语法通过数 | 149/151 | **149/151**，无新增失败 ✅ | 零新增 |

### 4.1 产物字节变化的 14 个文件（逐条归因）

| 变化 | 文件数 | 文件 | 归因 |
| :--- | ---: | :--- | :--- |
| `type` 的 `\` | 1 | `史上最牛X…` | F1（`cat /\jzcx.txt` → `cat //jzcx.txt`） |
| `for /d` 末元素 | 1 | `快速创建文件夹.bat` | F2（+ 去掉误加的 `shopt -s nullglob`） |
| `\` 后接变量 | 12 | `一键转移桌面…`、`多功能系统优化设置.cmd`、`按软件分类建立文件夹.cmd`、`提取IE缓存的指定文件.bat`、`改名复制文件.cmd`、`文件备份器V2.3修改版2.cmd`、`文件夹伪装.bat`、`生成包含…网页.bat`、`移动文件.bat`、`系统优化.bat`、`史上最牛X…`、`以文件夹名为名…` | F5 |
| `find` 绝对路径 | 2 | `查找最新的文件.bat`、`美化文件夹背景.bat` | F3 |

（`史上最牛X…` 与 `以文件夹名为名…` 同时命中多条，故合计 14 而非 16。）

**053 覆盖文件（§3.3）观察结果**：5 个里 **2 个字节未变**；
变化的 3 个（008 / 031 / 073）**全部是上述修复本身**，且 **5/5 `todo_count` 未变**。

---

## 5. 已知限制与残留（随版本披露）

| # | 限制 | 影响 | 处置 |
| :-: | :--- | :--- | :--- |
| **L1** | `%%~n` 的通用表达式仍是 `basename "${v%.*}"`，未加固 | 绝对路径后，只有**父目录某段含点**（如 `~/my.project/`）时才截错；比修复前（`find .` ⇒ 恒坏）严格更好但**未根除** | 彻底加固需改 `_modifier`（A1 邻域）⇒ 另开带防护的改动 |
| **L2** | `%%i` 的值由相对变绝对 | 依赖 `%%i` 文本形态的脚本行为会变（这是**向 cmd 靠拢**） | 按 minor 版本 v2.11.0 披露 |
| **L3** | `\` 后接变量仍有 **3 处可执行残留** | 2 处 = `全删.bat` 的 `\\?\` 前缀（**有意排除**）；1 处 = `PersonalReg=…:\\Personal\\%username%` 的 `\\` 连写（**有意排除**）。另有 7 行 `# TODO[REG]` 注册表键路径（`\` 在那里才正确） | 属"不做映射"（同盘符/UNC）。48 处 → 16 处，**可执行代码里再无"该转而未转"** |
| **L4** | **F6（新登记）**：`_convert_path_token` 的通配符回退可产出**未加引号的 glob** | `全删.bat` 的 `\\?\` 前缀若被转换，会得到 `rm -rf \/?/${1:-}` —— `?` 是活通配符，可能误删根下单字符目录。**本 session 通过排除 `\\?\` 规避**，但底层脆弱性仍在 | 下一 session 候选（与 F5 同级） |
| **L5** | F4/盘符行为**对用户无告警** | 把日志写到 `C:\logs\` 的脚本，在 Linux 上静默写进 `./C:/logs/` | 已登记于上一轮 D15；是否加告警属产品决策 |
| **L6** | `compare.py` 的 D15 规则**放宽了一处判别力** | 会掩盖"本该写到工作根之外、却以其它形态落进工作根"的写入（仅限首段恰为 `X:` 的路径） | 报告**逐条打印**该规则"掩盖了什么" |

---

## 6. 纪律自查

| # | 纪律 | 执行 |
| :-: | :--- | :--- |
| 1 | 保守 TODO 优于激进转换 | ✅ 否决了 `_expand_vars` 预转换（漂移 17 vs 13）；为 `\\?\` 前缀**主动加排除**以不引入 `rm -rf` 活通配符；F3 的保守 TODO 退路已写入诊断 §8.3 |
| 2 | 每个 commit 前跑全量 pytest | ✅ 4 个 commit 各自跑过 D1–D4 |
| 3 | 只读诊断先行 | ✅ 诊断阶段仓库零改动（`git status` 干净），所有补丁只打在 `/tmp` 副本 |
| 4 | 主动披露偏离 | ✅ **6 条**：commit 顺序（§2）、仪器变更 I1/I2/I3（§3.2）、限定 F5 范围（G2）、4 条断言重写（§4）、已知限制 L1–L6（§5）、诊断中新发现 F5/F6 |
| 5 | 只读任务严格只读 | ✅ 本轮是产品轨道，改动限于 `python/`+`tests/`+`research/behavior-tracking/` |
| 6 | 不发明映射 | ✅ `for /d` 规则由**真机指纹**导出；`%%~ni` 目标值由 W 产物 `samples.txt` 导出；`\\?\` 排除由 `全删.bat` 的**实测风险**导出 |
| 7 | 仪器先验证 | ✅ **本轮最关键**：靠它发现 L 采集器用的是已安装旧版（I1）；`cmp_measure.py` 索引零填充 bug、rglob 混入沙箱副产物两处假差异也是自检抓到的 |
| 8 | 锁定 HEAD | ✅ 起点 `9c096a4` 全程未动；4 个 commit 串行，每步记录回滚锚点 |
| 9 | 测试断言必须验证语义 | ✅ 4 条 `test_batch_for_r.py` 断言（原编码"相对路径/字面命令串"）重写为 `realpath` 归一 + 真跑断言；12 条新测试**全部**做运行语义断言 |

---

## 7. 与上一轮 `oracle-verdict.md` 结论的关系

上一轮的 P0 建议是"把 F1/F2/F3 交产品侧定级"，并说清代价：
**纯差分 oracle 的可判定面天然只有 A 类 4/151（2.6%）**，两侧盲区相同。

本轮**没有**改变那个边界 —— 仍然只有 5 个样本、仍是净差分。
能说的是：**这 5 个样本上，3 个真实缺陷已全部消除，且没有引入新的不一致。**
不能说的是：bat2sh 整体的正确率因此提高了多少。

上一轮的 P1 建议（对 3 个缺陷的**静态命中文件**逐个跑 oracle，把"波及面"从模式命中变成实测）
**本轮只完成了一半**：静态命中面已经用**全量产物签名**量化（不是源码正则），
但 **10 个命中文件仍未逐个跑 oracle**。这是明确的下一步。
